---
target: index.html
total_score: 32
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
timestamp: 2026-08-11T10-51-51Z
slug: index-html
---
Method: dual-agent (A: design-review sub-agent · B: detector/browser sub-agent)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | 조용한 거절(say()만)에서 cmdbar가 직전 명령에 멈춤; CONFLICT가 cmdbar에 `$` 접두로 표시 |
| 2 | Match System / Real World | 4 | 은유가 데이터 인코딩 그 자체; 실제 git 문구 인용 |
| 3 | User Control and Freedom | 3 | 모드 전환이 확인 없이 전체 상태 + undoStack 파괴 |
| 4 | Consistency and Standards | 3 | 자기 규칙 위반 3건 (cmdbar CONFLICT `$`, 거절 단계 ✓, #9AA6B4) |
| 5 | Error Prevention | 3 | 가드는 촘촘하나 모드 전환 데이터 손실 무방비 |
| 6 | Recognition Rather Than Recall | 3 | 시작 시 빈 select 3개가 죽은 컨트롤로 보임 |
| 7 | Flexibility and Efficiency | 3 | 방향키·#step 해시·진행 막대 점프 적정 |
| 8 | Aesthetic and Minimalist Design | 3 | 자유 모드 15개 컨트롤 동시 노출; 시나리오 모드 우측 열 진공 |
| 9 | Error Recovery | 4 | 거절 파이프라인은 제품의 논지 그 자체 — 모범 수준 |
| 10 | Help and Documentation | 3 | 기호 설명이 앱 내 재접근 경로 없음 |
| **Total** | | **32/40** | **Good** |

## Design Specificity Verdict

**Bespoke.** 노선색 6종 = 실제 서울 지하철 노선색, 구역 = 커밋의 소재, 선 종류 = 상태, `$`/`#`/무접두 로그 규율 — 전부 이 제품의 성공 기준("로그를 터미널에 옮겨 친다")에서만 나오는 결정. 범용 템플릿 냄새 없음.

**Deterministic scan** (degraded regex 모드 — HTML 파서 모듈 부재로 undercount): 28건 = design-system-color 17 · font-size 7 · radius 3 (advisory) + bounce-easing 1 (warning). bounce-easing은 의도된 시그니처 등장 모션(DESIGN.md 문서화)이라 오탐. color/font-size 다수는 DESIGN.md frontmatter에 안 올라간 상태색 보조값들 — 디자인 결함이 아니라 문서화 갭. font-size 7건은 layout에 결합된 마이크로 라벨(문서화된 예외).

**Visual overlays**: live-server.mjs 기동 실패(타임아웃)로 detect.js 오버레이 미주입. python 대체 서버로 주입 가능성 자체는 확인(프리플라이트 성공)했으나 detect.js 엔드포인트가 없어 생략.

## Overall Impression

교육 논리가 시각 시스템으로 완전히 번역된 드문 사례. 거절·충돌 처리(H9=4)는 이 제품의 최고 자산. 가장 큰 기회: 자기 규칙을 깨는 세 곳(CONFLICT `$`, 390px 오버플로, #9AA6B4)을 메우고, 번호가 약속한 학습 동선을 시나리오 엔딩에서 잇는 것.

## What's Working

1. **거절 파이프라인 다중 채널** — cmdbar 빨간 배경 + 로그 e줄 + note 복구 안내 + snap() 정합성. "거절은 교재"가 세 층위에서 구현됨.
2. **원격 head/known 이중 상태 시각화** — origin/main 이름표가 "내가 마지막 확인한 위치"에 붙는 것. 기존 git 도구에 없는 독자적 기여.
3. **충돌 상태 기계** — ①②③ 스테퍼, 선택 유지, 학생 선택 비덮어쓰기, 재선택 시 로그 교체.

## Priority Issues

- **[P1] 390px 가로 오버플로**: `#freePanel` 기본 규칙(208행) `grid-template-columns:1fr` = minmax(auto,1fr) 함정 — 1400px 쿼리에서만 고치고 기본에서 빠뜨림. scrollWidth 392 vs clientWidth 376. 수정: `minmax(0,1fr)`. → /impeccable adapt 또는 polish
- **[P1] cmdbar가 CONFLICT를 `$ CONFLICT (content)…`로 표시**: 로그 종류 `x`에 cmdbar 스타일·건너뛰기 없음(1559행이 x를 잡음). Terminal-Truth 규칙을 화면 최대 글씨가 정면 위반 — 학생이 CONFLICT를 터미널에 옮겨 칠 위험. 수정: `"o"`와 `"x"` 함께 건너뛰거나 `.cmdbar.x` 스타일 추가. → /impeccable polish
- **[P2] 모드 전환이 확인 없이 상태 파괴**: setMode→fresh()가 undoStack까지 소거. 오터치 복구 수단 0. → /impeccable harden
- **[P2] 빈 상태 문구 #9AA6B4 (1310행)**: 대비 ~2.9:1, 자기 Contrast-Floor 규칙 위반. 수정: var(--ink-soft). → /impeccable polish
- **[P2] 접근성 기초 묶음**: html lang 미지정, #note aria-live 없음, 투어 dialog/포커스 미처리, 진행 막대 seg 8×12px. → /impeccable audit + harden

## Persona Red Flags

**Jordan (첫 사용자)**: 자유 모드 기본 진입 시 15개 컨트롤; 미리 채워진 입력 + 검은 commit 버튼 → 첫 클릭이 빨간 거절일 확률; 빈 select 3개가 고장처럼 보임; note 지시문("아래 '다음 단계'")과 CTA가 대각선 반대.

**Sam (접근성)**: lang 미지정 + #note 무음 = 핵심 콘텐츠(거절 설명)가 스크린리더에 전달 안 됨; 투어 중 Space 가로채기로 버튼 활성화 불가; 포커스가 마스크 뒤 어둠 속을 돎.

**Casey (모바일)**: 390px 가로 흔들림(P1); ≤900px에서 cmdbar 숨김 — 거절의 최강 신호가 폰에 없음, 로그는 세 화면 아래; 투어 말풍선이 하단 40% 상시 점유.

## Minor Observations

- 1440px 투어 4단계에서 말풍선이 밝힌 대상(범례·cmdbar)을 덮음
- 마스크 클릭=일시정지는 발견 불가능한 제스처
- `.log .o` opacity .8이 실효 대비 ~3.5로 Contrast-Floor 소폭 하회
- Esc가 투어와 #step 해시를 함께 지움 — 재개 지점 소실
- 진행 순서에 "현재 단계" 강조 없음 (done/미완 2단계뿐)
- 거절 단계가 성공과 같은 ✓ (P3)
- setMode가 msg 입력을 안 비워 Commit 시나리오 시작 화면에 승객 점 잔재 (P3)

## Questions to Consider

1. 첫 방문 기본값이 왜 자유 모드인가 — 자유 모드 초기 note가 "처음이면 ▶ 자동 안내"를 가리키기만 해도 되지 않나?
2. 모바일에서 거절이 빨갛지 않아도 되는가 — 모바일 전용 축약 cmdbar(32px)가 필요한가?
3. 번호(1→7)는 동선을 약속하는데 매 시나리오가 회색 비활성 버튼으로 끝난다 — "다음: 2 Merge →"가 번호 체계의 완성 아닌가?
