---
target: index.html
total_score: 34
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 1
timestamp: 2026-08-11T11-43-58Z
slug: index-html
---
Method: dual-agent (A: design-review sub-agent · B: detector/browser sub-agent) — 2차 측정

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | cmdbar·note·흐름 띠·cfxStep — 상태 표면화가 최강점 |
| 2 | Match System / Real World | 4 | 은유 = 데이터 인코딩, 실제 git 문구 인용 |
| 3 | User Control and Freedom | 3 | 처음부터가 무확인 + undoStack 소거로 복구 불가 |
| 4 | Consistency and Standards | 3 | P0: tour-cta 스타일이 .modes button 특정도에 덮여 소실 |
| 5 | Error Prevention | 3 | 가드 촘촘, 처음부터만 무방비 |
| 6 | Recognition Rather Than Recall | 4 | select 나열·cfxStep·범례 고정 — 외울 것 없음 |
| 7 | Flexibility and Efficiency | 3 | 진행 막대 30칸 전부 Tab 순회 — 한 바퀴 34탭 |
| 8 | Aesthetic and Minimalist Design | 3 | 자유 모드 컨트롤 21개 동급 노출 |
| 9 | Error Recovery | 4 | 거절 = 원인+다음 행동+실제 git 인용. 모범 |
| 10 | Help and Documentation | 3 | 풍부하나 입구(자동 안내 CTA)가 P0으로 파묻힘 |
| **Total** | | **34/40** | **Good (상단)** |

## Design Specificity Verdict

**Distinctive.** 구역=소재지, 선 종류=상태, 알약=참조, $/#/무접두=옮겨 칠 수 있는가 — 거의 모든 픽셀이 git 개념에 답한다. 유일한 균열이 P0: 가장 눌러야 할 버튼이 가장 안 눌러 보이게 렌더.

**Deterministic scan** (degraded regex, undercount 경고): 28건 — bounce 1(오탐: 문서화된 시그니처 모션), color 17, font-size 7, radius 3. 1차와 동일 구성. 검증: JS 안 SVG 문자열 4건·CSS 변수 폴백 1건·오버레이 알파 3건은 regex 폴백 한계, 나머지는 DESIGN.md frontmatter 미등재 상태색(문서화 갭). 코드 결함 아님.

**Visual overlays**: live-server 기동 실패(전례 동일) → python 대체, 주입 가능성 프리플라이트 성공했으나 detect.js 소스 없어 미주입.

## Overall Impression

1차 32 → 2차 34. 1차 P1·P2가 전부 닫혔고(오버플로·CONFLICT 표기·모드 전환·대비·모바일 cmdbar — 재발 없음, A가 freeSnap·Esc 해시·✗ 표시를 실기로 확인하고 "잘된 것"에 올림), 새로 찾은 것이 P0 하나: CSS 특정도 싸움.

## What's Working

1. 거절의 시각 언어 — cmdbar.e·로그 빨간 줄·진행 순서 ✗가 한 사건을 세 자리에서 같은 색으로.
2. 상태 복원 배려 — freeSnap 복원 문구, Esc의 #step 보존, 포커스 반환까지 실기 확인.
3. rebase 유령 연출 — 해시 비교가 화면에서 성립.

## Priority Issues

- **[P0] 자동 안내 CTA 스타일 소실**: `#btnTour`가 `.modes` 안이라 `.modes button`(0,1,1)이 `.tour-cta`(0,1,0)를 덮음. 실측: 흰 배경·회색 글씨 — 미선택 알약과 동일. DESIGN.md tour-cta 정의·One-Pink Rule 핵심 사용처가 죽어 있음. 수정: `.modes .tour-cta` 재선언 1줄. → /impeccable polish
- **[P1] 진행 막대 30칸 전부 Tab 순회**: 트랩 안 한 바퀴 34탭. roving tabindex(seg tabindex=-1 + 화살표 이동). → /impeccable harden
- **[P2] 모바일 시나리오 루프 — 그림이 화면 밖**: 390px에서 다음 단계 y≈1705 vs 노선도 y≈180. 단계 실행 직후 지도 스크롤 또는 sticky cmdbar. → /impeccable adapt
- **[P2] 처음부터 무확인 + 복구 불가**: reset 직전 상태를 undoStack에 한 칸 남기면 무확인 철학 유지하며 안전. → /impeccable harden
- **[P3] 시나리오 종료 막다른 골목**: SCEN에 next 필드 — "다음은 4번 Push" 넛지. → /impeccable onboard

## Persona Red Flags

**Jordan**: P0으로 입구 버튼이 배경에 녹음; 첫 화면부터 대합실에 분홍 점(미리 채운 예시 부작용).
**Sam**: 34탭 순회; 유령 커밋 대비 1.9:1은 의도된 예외인데 DESIGN.md에 예외 조항 없음.
**Casey**: 버튼↔그림 분리; seg 폭 ~8px; 노선도 자동 스크롤로 첫 커밋 잘림.

## Minor Observations

- 거절 연타 시 빨간 줄 중복(실제 터미널과 동일 — 결함 아님)
- 시나리오 모드 우측 열 여백 — "다음 시나리오" 넛지 자리로 적합
- 헤드리스에서 clip-path 마스크 합성 안 됨(실브라우저 무관)
- 기본기: reduced-motion·esc()·포커스 링 일관

## Questions to Consider

1. 첫 방문자를 안내로 이끄는 것 — P0 수정으로 충분한가, 1회성 배너가 필요한가?
2. 자유 모드가 학생과 함께 자라야 하나 — 전부 보여주기 vs 순서대로 열기?
3. 뒷자리 가독성이 노선도 안(역 메시지 14px)까지 적용돼야 하나 — 시연용 글자 확대 토글?
