# CLAUDE.md — Git 노선도 작업 기록

Git 브랜치 개념을 지하철 노선도 은유로 시각화한 교육용 단일 HTML 웹앱.
명지전문대 AI게임학과 강의 자료.

- 저장소: https://github.com/abback-go/GitMap
- 배포: https://abback-go.github.io/GitMap/ (GitHub Pages, `main` 브랜치)
- 본체: `index.html` 한 파일 (약 960줄)

---

## 1. 지켜야 할 제약

작업할 때 항상 유효한 규칙이다. 어기면 산출물이 못 쓰게 된다.

1. **단일 HTML 파일 구조 유지.** 외부 JS/CSS로 분리하지 않는다 (Claude 아티팩트 이식을 염두에 둔 구조)
2. **`localStorage` / `sessionStorage` 사용 금지**
3. **한국어 UI 유지**
4. **디자인 방향을 바꾸지 않는다** — 지하철 노선도 은유, 색 팔레트, Pretendard 폰트
5. **산출물은 작업 폴더에만 기록한다.** 바탕화면 등 폴더 밖 경로는 요청 없이 건드리지 않는다
6. 코드 수정 후에는 **반드시 Playwright로 렌더링 → 스크린샷 → 눈으로 확인**. 스크린샷 없이 "수정했습니다"라고 보고하지 않는다
7. 데스크톱(1440px)과 모바일(390px) **두 폭 모두** 확인

## 2. 검증 방법

`file://` 프로토콜은 Playwright MCP가 차단한다. 로컬 정적 서버를 띄워서 확인한다.

```bash
python -m http.server 8765 --bind 127.0.0.1   # run_in_background: true
# http://127.0.0.1:8765/index.html
```

- 스크린샷 저장 경로는 작업 폴더 안이어야 한다 (`shots/`). 폴더 밖은 Playwright가 거부한다
- `shots/`, `.playwright-mcp/`는 `.gitignore`로 제외되어 있다
- 상태 조작은 `browser_evaluate`로 전역 함수를 직접 호출하는 게 빠르다 (`cmdCommit`, `cmdBranch`, `setMode`, `tourStart` 등 모두 전역)
- **주의**: 헤드리스 환경에서 CSS transition / rAF가 지연되어 `getBoundingClientRect()`가 애니메이션 중간값을 돌려줄 때가 있다. 위치를 정밀 측정할 때는 인라인 스타일 값(`tip.style.top`)을 읽어야 신뢰할 수 있다

## 3. 코드 구조

### 상태 (`S`)

```js
S = {
  commits: { [id]: { id, parents:[], lane, msg, color, merge?, from? } },
  order:   [id, ...],        // 생성 순서 = 위상 정렬 순서
  branches:{ [name]: { name, head, lane, color, remote? } },
  head:    "main",           // 현재 브랜치 이름 (원격은 절대 들어가지 않음)
  laneNext: 0,
  log:     [{ t, k }]        // k: 없으면 명령, "e" 거절/오류, "n" 명령 아닌 설명
}
```

- `undoStack`은 `clone(S)`의 배열 (최대 60)
- `seenNodes` / `seenEdges`는 등장 애니메이션 1회 재생용 추적 집합
- 원격은 `S.branches["origin/main"]`에 `remote:true`로 들어간다. 브랜치 구조를 재사용하므로 이름표 렌더·도달성 계산이 그대로 동작한다

### 렌더 파이프라인 (`render()`)

순서에 의존성이 있다. 중간에 끼워 넣을 때 주의.

1. `reach` — 모든 브랜치 head의 조상 집합. 여기 없으면 "버려진 커밋"
2. `depth` — 부모 최대 depth + 1 → 가로 칸 번호
3. `rowKey` / `rows` / `rowOf` — 세로줄 배치. `(살아있음? "L":"G") + lane`으로 묶고 **살아있는 줄 먼저, 그다음 버려진 줄** 순으로 정렬해 행 번호를 부여. 빈 lane은 자동 압축된다
4. `labels` — 커밋별 브랜치 이름표 묶음, `stackMax`, 동적 `ROW_H`
5. `gapAt` / `colX` — 이름표가 다음 칸 역을 덮지 않도록 열 간격 확대
6. `pos` — 최종 좌표, `maxX` / `maxY`
7. 줄 안내선 → 선로(edge) → 역(node) → 이름표+HEAD 순으로 그린다 (z-order가 이 순서에 의존)

### 좌표 규칙

| 상수 | 값 | 뜻 |
|---|---|---|
| `COL_W` | 112 | 기본 가로 칸 간격 |
| `ROW_H0` | 104 | 기본 세로줄 간격 (이름표가 쌓이면 늘어남) |
| `LBL_GAP` | 23 | 이름표 세로 간격 |
| `TOUR_DOCK` | 20 | 노선도 패널과 안내 말풍선 사이 고정 여백 |

- `ROW_H = max(104, (stackMax-1)*23 + 68)` — 쌓인 이름표가 아래 줄 글자와 겹치지 않게
- 커밋 원 위쪽에 **메시지 → 해시 → 원** 순으로 배치. 아래쪽은 비워 둔다
- 이름표는 원 오른쪽에 세로로 쌓이고, 현재 브랜치 이름표 오른쪽에 HEAD 뱃지가 붙는다

## 4. 알려진 함정 (반복 금지)

- **SVG `<g transform="translate()">`에 CSS `transform: scale()` 애니메이션을 직접 걸면 translate가 덮어써져 요소가 (0,0)으로 날아간다.** translate용 그룹과 scale 애니메이션용 그룹을 반드시 분리할 것 (`g` / `gi` 구조가 그 때문)
- `svg { min-width: 100% }`를 주면 viewBox 대비 늘어나며 요소가 잘린다
- 노드 위쪽에 텍스트, 아래쪽은 여백 — 이 배치를 유지. 위아래 여백 부족으로 원이 잘리지 않는지 매번 확인
- 사용자 입력(커밋 메시지, 브랜치 이름)을 `innerHTML`에 넣기 전 **반드시 `esc()`** 를 통과시킬 것
- Bash 도구에서 PowerShell here-string(`@'...'@`)은 동작하지 않는다. 여러 줄 커밋 메시지는 heredoc(`<<'EOF'`)을 쓸 것

---

## 5. 작업 이력

원본은 HEAD 표시·이름표 겹침·모바일 미검증 3개 버그를 가진 상태였다. 아래는 그 이후 전체 변경 내역.

### 1차 — 최초 3개 버그

**A. HEAD가 어느 브랜치를 가리키는지 안 보임 (최우선)**
`git switch -c` 직후 이름표만 두 개 붙고 HEAD는 커밋 아래 그대로 있어, 안내 문구("HEAD가 새 노선으로 옮겨갔다")와 그림이 어긋났다.

- 커밋 아래 HEAD 뱃지를 없애고 **HEAD → 브랜치 이름표 → 커밋** 3단 구조로 변경
- 이름표를 커밋별로 묶어(`labels`) 오른쪽에 세로로 쌓고 연결선으로 커밋과 연결
- HEAD 뱃지는 현재 브랜치 이름표 오른쪽에 붙고 화살표가 그 이름표를 가리킨다
- 현재 브랜치 = 진한 테두리 + `font-weight:800` + 불투명 / 나머지 = `opacity:.5`

**B. 브랜치 이름표 겹침**

- `ROW_H`를 상수에서 계산값으로 (쌓인 개수에 따라 확대)
- 열 간격 동적 확대 (`gapAt`)
- 이름표 뒤 흰 바탕 rect — 선로 위에 얹혀도 읽힘
- `maxX`/`maxY`가 이름표+HEAD 폭까지 포함하도록 수정
- 커밋 없는 lane의 안내선은 그리지 않음

**C. 모바일 미검증**

- `tourPlace()`: 대상이 화면 밖이면 `scrollIntoView`, 말풍선은 여백 넓은 쪽에, 부족하면 `scrollBy`로 자리 확보 (기존엔 스크롤을 안 해서 390px에서 조작 패널 단계 스포트라이트가 화면 밖이었다)
- 노선도 가로 자동 스크롤: 기본은 최신 역, HEAD가 왼쪽으로 잘리면 HEAD 쪽으로

### 2차 — rebase 유령 커밋 + 리뷰 발견분

- **rebase 후 버려진 커밋이 새 커밋에 가려지던 문제** → `rowKey`로 살아있는 줄/버려진 줄을 분리. 부수 효과로 빈 lane 압축도 됨
- 자동 안내 2단계 문구 오류 정정 (해시/메시지 위아래 순서가 실제와 반대였다)
- **안내를 닫아도 예약된 단계 동작이 실행되던 버그** — `clearTimeout(tTimer)`가 죽은 코드였다. `tLater`/`tClear`로 예약 타이머를 모아 관리
- **XSS** — 커밋 메시지·브랜치 이름이 `innerHTML`로 들어갔다. `esc()` 추가
- 빈 커밋 메시지로 커밋하면 로그에 `git commit -m ""`이 남던 문제
- 브랜치 이름 검증 추가 (git `check-ref-format` 핵심 규칙, 한글은 실제 git처럼 허용)
- 현재 브랜치로 `이동` 시 무반응 → 안내 문구
- `▶ 자동 안내` 버튼에 `aria-pressed`가 붙던 문제
- 범례에 HEAD 항목 추가

### 3차 — rebase 정확성 + 안내 재실행

- **rebase가 병합 커밋을 복제하던 문제** → 실제 git처럼 병합 커밋은 replay에서 제외 (`--rebase-merges` 없을 때의 동작). 버려진 병합 커밋은 자동으로 점선 줄로 내려간다
- 안내에서 뒤로 갔다 앞으로 오면 단계 동작이 중복 실행되던 문제
- 버려진 병합 커밋의 가운데 점만 원색으로 남던 시각 결함

### 4차 — 안내 재생 제어

- **이전 버튼을 눌러도 노선도가 되감기지 않던 문제** → 단계 진입 시점의 상태를 `tSnap[]`에 스냅샷하고 복원. 동작이 있는 단계는 재실행되므로 이후 스냅샷을 무효화. 이 구조 덕분에 3차의 중복 실행 방지 플래그(`tRan`)는 제거됨
- **일시정지해도 단계 시계가 계속 흘러, 이어보기를 누르면 바로 다음 단계로 넘어가던 문제** → `tT0`/`tPauseAt`로 멈춘 시간만큼 시작 시각을 미룸

### 5차 — 안내 말풍선 위치

- 여백 넓은 쪽으로 위아래를 오가던 말풍선을 **노선도 패널 바로 아래에 고정 여백 20px**로 고정 (넓은 화면)
- 가로 중앙도 화면이 아니라 **노선도 폭 기준** — 화면 중앙에 두면 오른쪽 조작판을 30px 물었다
- 좁은 화면(<900px)은 붙일 빈 자리가 없어 화면 아래 22px 고정 + 페이지 여백 확보 방식 유지
- 뷰포트가 낮으면 여백이 줄어들 뿐 말풍선이 화면 밖으로 나가지 않도록 클램프

### 6차 — 10단계 서사 불일치

안내 10단계가 "팀원이 main에 커밋했다"고 설명하면서 실제로는 내가 main에서 커밋하고 있었다. 로그에는 `git switch main` + `git commit`이 남는데 19단계에서 "이 로그를 그대로 터미널에 치면 같은 결과"라고 말하는 자료라 그냥 둘 수 없었다.

1차로 문구를 "`git pull`로 받아오지만 여기서는 흉내 낸다"로 고치고 관찰 단계를 추가했으나, 사용자 지적으로 **실제 원격 저장소를 구현**하는 방향으로 확대.

### 7차 — 원격 저장소 구현

- `origin/main`을 `remote:true` 브랜치 항목으로 추가. 추적 대상은 main 하나 (실제 git도 새 로컬 브랜치는 push 전까지 원격에 없다)
- 선로 색은 main과 동일 ("노선 = 색" 규칙 유지), **속이 빈 이름표**로만 원격을 구분
- 자유 모드 버튼 3개: `팀원이 커밋` / `받아오기 · pull` / `올리기 · push`
  - pull: 뒤처졌으면 fast-forward, 갈라졌으면 병합 커밋 (`git pull` = fetch + merge)
  - push: 원격이 조상이면 전진, 아니면 **거절**(non-fast-forward, 로그에 빨강)
  - 가드: main이 아닌 브랜치, 이미 최신, 올릴 것 없음, 내 쪽이 앞섬
- 시나리오 모드(Merge/Rebase)는 원격을 만들지 않는다 (`fresh(withRemote)`)
- **렌더러 버그 2건 수정** (이 작업에서 드러남)
  - 원격이 앞선 상태에서 로컬 커밋 시 두 커밋이 같은 칸에 겹치던 문제 → `doCommit`에 갈라짐 규칙 추가 (같은 줄에 이미 선로가 뻗어 있으면 새 줄로, 색은 유지)
  - 열 간격 계산이 "부모-자식인 다음 역"만 봐서, rebase 후 `origin/main`이 무관한 다음 역과 겹치던 문제 → **같은 줄 다음 칸이 차 있는지**로 판정하도록 교체
- 안내 21단계로 재구성. **초반 push 단계가 없으면 origin이 뒤처져 12단계 pull이 fast-forward가 아니라 병합이 되어 버린다**는 문제를 발견해 6단계에 push를 넣어 해결

```
4 커밋 → 5 관찰 → 6 원격에 올려두기(push) → 7 브랜치 → 8 관찰 → 9 기능 커밋 2회
→ 10 main으로 이동 → 11 팀원이 원격에 커밋 → 12 원격만 앞서 나감(관찰)
→ 13 받아오기(pull, fast-forward) → 14 두 노선 갈라짐(관찰) → 15 병합 → 16 환승역(관찰)
→ 17 올려서 공유(push) → 18 기호 → 19 로그 → 20 시나리오 → 21 마무리
```

결과적으로 로그가 그대로 재현 가능한 협업 세션이 되었고, "흉내 냅니다" 류의 면피 문구가 사라졌다.

### 8차 — 저장소 구성

- `git init` → GitHub `abback-go/GitMap`(PUBLIC, 비어 있던 상태)에 푸시
- README 작성 → 이후 **학생용으로 재작성** (맨 위 바로 열기 링크, 자동 안내부터 시작하는 순서, 화면·기호 읽는 법. 개발 내용은 `<details>` 접이식)
- GitHub Pages 활성화 (`main` 브랜치 root)
- `git-branch-map.html` → **`index.html`로 이름 변경** — Pages 루트에서 바로 열리도록. 학생 링크가 짧아졌다
- `develop` 브랜치 생성. 이후 작업 방식은 아래 참조

---

## 6. 브랜치 운영

| 브랜치 | 역할 |
|---|---|
| `main` | 배포본. GitHub Pages가 서빙한다. **머지 = 배포** |
| `develop` | 작업용 |

```bash
git switch develop
# 수정 & 커밋
git switch main
git merge develop
git push
```

두 브랜치는 **같은 파일 목록**을 유지한다. 브랜치별로 파일을 나누면 머지 때마다 충돌한다.

Pages는 브랜치 하나만 서빙하므로 develop의 변경은 배포에 반영되지 않는다. 작업 중 확인은 로컬 서버로.

## 7. 미결 / 판단 보류

- **`git fetch` 버튼 없음.** 노선도가 원격과 내 저장소를 한 그림에 함께 그리는 방식이라(`git log --all` 관점) fetch 단계가 그림에서 드러나지 않는다. 13단계 문구에서 "pull = fetch + merge"로만 설명한다. 별도로 보이려면 "아직 안 가져온 커밋"을 흐릿하게 그리는 표현이 필요한데 개념 부담이 커서 보류
- **rebase가 여러 번 일어나면** 버려진 커밋들이 한 줄에 모여 칸이 겹칠 수 있다. 현재 구조는 버려진 줄을 원래 lane별로 구분하지만 서로 다른 rebase의 유령이 같은 칸에 오는 경우까지는 검증하지 않았다
- **파비콘 없음** — 배포 페이지에서 `favicon.ico` 404가 콘솔에 남는다. 동작에는 영향 없음
- 자동 안내 21단계 전체 재생은 약 2분 30초. 수업에서 길면 단계별 지속시간(`tourShow`의 `dur` 계산)을 줄이면 된다
