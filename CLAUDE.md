# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

Git 브랜치 개념을 지하철 노선도 은유로 시각화한 교육용 단일 HTML 웹앱.
명지전문대 AI게임학과 강의 자료.

- 저장소: https://github.com/abback-go/GitMap
- 배포: https://abback-go.github.io/GitMap/ (GitHub Pages, `main` 브랜치)
- 본체: `index.html` 한 파일 (약 1,420줄)

---

## 1. 지켜야 할 제약

작업할 때 항상 유효한 규칙이다. 어기면 산출물이 못 쓰게 된다.

1. **단일 HTML 파일 구조 유지.** 외부 JS/CSS로 분리하지 않는다 (Claude 아티팩트 이식을 염두에 둔 구조)
2. **`localStorage` / `sessionStorage` 사용 금지** — 실습실 공용 PC에서 앞 사람 상태가 남으면 안 된다. 매번 `최초 커밋` 하나에서 깨끗하게 시작하는 게 수업 진행의 전제다. 상태 복구가 필요하면 URL 해시(`#step=13`)를 쓴다
3. **한국어 UI 유지.** 단 **버튼 라벨만 예외** — 실제 git 명령에 대응하는 버튼은 명령어 그대로 쓴다(`add` `commit` `switch -c` `switch` `merge` `rebase` `fetch` `pull` `push`). 학생이 터미널에 칠 문자열과 버튼이 1:1로 맞아야 하기 때문이다. **git 명령이 아닌 버튼은 한글로 남긴다**(`팀원이 커밋` `PR 병합` `한 단계 되돌리기` `처음부터`) — 한글이라는 것 자체가 "이건 git 명령이 아니다"라는 신호다. 설명·안내·머리글은 전부 한국어. 명령 버튼에는 한글 뜻을 `title`로 달아 둔다
   - **PR은 `PR(열기)` / `PR(병합)` 두 버튼으로 나눠 둔다.** 병합 버튼만 있으면 "PR = 합치는 것"으로 읽힌다. 여는 쪽은 저장소를 전혀 안 바꾸고 설명만 띄우므로, 버튼 둘이 나란히 있는 것 자체가 "요청과 병합은 다른 일"을 말해 준다. 괄호 형태라 둘 다 PR의 일부로 묶여 보인다 — **한쪽만 이름을 바꾸지 말 것**
   - **라벨을 바꾸면 버튼을 이름으로 부르는 문구도 함께 고칠 것** — 거절 안내(`cmdCommit`·`cmdPush`·`cmdSwitch`·`cmdPR`·`cmdFetch`)와 안내 9단계 본문이 버튼 이름을 직접 부른다. 안 고치면 "올리기를 누르세요"인데 화면엔 `push`가 있다
4. **디자인 방향을 바꾸지 않는다** — 지하철 노선도 은유, 색 팔레트, Pretendard 폰트
5. **산출물은 작업 폴더에만 기록한다.** 바탕화면 등 폴더 밖 경로는 요청 없이 건드리지 않는다
6. 코드 수정 후에는 **반드시 브라우저로 렌더링 → 스크린샷 → 눈으로 확인**. 스크린샷 없이 "수정했습니다"라고 보고하지 않는다
7. 데스크톱(1440px)과 모바일(390px) **두 폭 모두** 확인

## 2. 검증 방법

브라우저 MCP는 세션마다 다르다. **먼저 붙어 있는 도구를 확인하고 그에 맞춰 진행한다.**

| 도구 | 페이지 열기 | JS 실행 |
|---|---|---|
| Playwright MCP (`mcp__playwright__*`) | `browser_navigate` | `browser_evaluate` |
| Chrome MCP (`mcp__claude-in-chrome__*`) | `tabs_create_mcp` / `navigate` | `javascript_tool` |

Chrome MCP는 `claude-in-chrome` 스킬을 먼저 호출하고, 도구가 지연 로딩 상태면 `ToolSearch`로 필요한 것을 **한 번에** 받아온다.

`file://`은 브라우저 MCP가 차단하므로 로컬 정적 서버를 띄운다.

```bash
python -m http.server 8765 --bind 127.0.0.1   # run_in_background: true
# http://127.0.0.1:8765/index.html
```

- 스크린샷 저장 경로는 작업 폴더 안이어야 한다 (`shots/`). 폴더 밖은 거부된다
- `shots/`, `.playwright-mcp/`는 `.gitignore`로 제외되어 있다. 스크린샷은 저장소에 넣지 않는다
- 상태 조작은 UI 클릭보다 **전역 함수 직접 호출**이 빠르다 (아래 3절 표)
- **주의**: 헤드리스 환경에서 CSS transition / rAF가 지연되어 `getBoundingClientRect()`가 애니메이션 중간값을 돌려줄 때가 있다. 위치를 정밀 측정할 때는 인라인 스타일 값(`tip.style.top`)을 읽어야 신뢰할 수 있다

## 3. 코드 구조

### 상태 (`S`)

```js
S = {
  commits: { [id]: { id, parents:[], lane, msg, color, merge?, from? } },
  order:   [id, ...],        // 생성 순서 = 위상 정렬 순서
  branches:{ [name]: { name, head, lane, color, remote?, known? } },
  head:    "main",           // 현재 브랜치 이름 (원격은 절대 들어가지 않음)
                             // 빈 문자열이면 "아직 내 저장소가 없다" (clone·commit 시나리오 시작)
  origin:  null,             // 등록된 원격 주소(.git/config). 서버에 무엇이 올라가 있는지와 다른 축이다
  staged:  null,             // git add로 담긴 변경 설명. 커밋하면 비워진다
  laneNext: 0,
  log:     [{ t, k }]        // k: 없으면 명령, "e" 거절/오류, "n" 명령 아닌 설명,
                             //    "o" 명령이 뿌린 출력 ($·# 없는 흐린 줄)
}
```

- `undoStack`은 `clone(S)`의 배열 (최대 60)
- `seenNodes` / `seenEdges`는 등장 애니메이션 1회 재생용 추적 집합
- `prevPos`는 **직전 render의 역 좌표**(`id → {x,y}`). 자리가 바뀐 역을 이전 자리에서 새 자리로 옮겨 그린다 — `pull`·`fetch`로 받아온 역이 서버 구역에서 올라오는 장면이 이것이다. `seenNodes`를 비우는 자리(`setMode`·`처음부터`·`tourRestore`)에서 **함께 비워야** 한다. 안 비우면 모드를 바꿀 때 엉뚱한 역이 날아다닌다
- 원격은 `S.branches["origin/<이름>"]`에 `remote:true`로 들어간다. 브랜치 구조를 재사용하므로 이름표 렌더·도달성 계산이 그대로 동작한다
- **원격만 `head` / `known` 두 개를 가진다.** `head`는 서버의 실제 위치, `known`은 내가 `fetch`로 받아 아는 위치. 둘이 벌어진 구간이 "아직 받지 않은 역"이고, 서버 띠의 이름표는 `known`에 붙는다
- 처음엔 `origin/main`만 있다. 다른 브랜치는 그 브랜치에서 `cmdPush()`를 해야 `origin/<이름>`이 생긴다 (`git push -u`). 헬퍼는 `remoteOf(name)` / `remotes()`
- **`S.origin`(주소 등록)과 `origin/*` 브랜치(서버에 올라간 것)는 다른 축이다.** 실제 `git remote add`는 `.git/config`에 URL만 적고 네트워크를 쓰지 않는다 — 직후 `git branch -r`는 아무것도 출력하지 않는다. 원격 추적 이름표는 `push -u`/`fetch`/`clone`이 만든다. 이 구분이 Push 시나리오 1·2단계의 교육 포인트이므로 **`cmdRemoteAdd()`에서 `origin/main`을 만들면 안 된다**

### 조작 API (전부 전역)

브라우저에서 JS로 직접 호출해 상태를 만든다. UI 클릭보다 빠르고 정확하다.

| 함수 | 동작 |
|---|---|
| `cmdInit()` | 빈 폴더를 저장소로 만든다(`git init`). `main` 브랜치만 생기고 커밋은 없다 |
| `cmdAdd()` | 입력칸 내용을 담는다(`git add .`). `S.staged`에 들어간다 |
| `cmdCommit(msg, auto)` | 담긴 게 없으면 **거절**. `auto=true`면 담기까지 한 번에 (시나리오·자동 안내용) |
| `cmdBranch(name)` / `cmdSwitch(name)` | 브랜치 생성(+이동) · 이동. `cmdSwitch`는 **로컬에 없고 원격에만 있는 이름**이면 추적 브랜치를 만들며 이동한다 |
| `cmdClone()` | 원격 전체를 로컬로 복사 + 기본 브랜치 생성. `S.head`가 비어 있을 때만 동작 |
| `cmdBranchList()` | `git branch -a`. 상태를 바꾸지 않고 서버에만 있는 노선을 알린다. 저장소가 없으면(`S.head`가 빔) 거절한다 |
| `cmdRemoteAdd()` | `S.origin`에 주소만 등록한다. **원격 브랜치는 안 만든다** — 서버 구역이 빈 채로 열린다 |
| `cmdMerge(name)` / `cmdRebase(name)` | 병합 · 재배치 |
| `cmdTeamCommit(msg)` | 팀원이 `origin/main`에 커밋 (내 브랜치도 `known`도 안 움직임) |
| `cmdPROpen(name)` | PR 열기. **상태를 안 바꾼다** — 로그·설명만 남긴다. 자유 모드 버튼은 `PR(열기)` |
| `cmdPR()` | 서버에서 `origin/<기능>`을 `origin/main`에 병합. 로컬은 안 움직인다 |
| `cmdFetch()` | 모든 원격의 `known`을 `head`로. 내 브랜치는 안 건드림 |
| `cmdPull()` / `cmdPush()` | **현재 브랜치**의 원격과 동기화. push는 원격이 없으면 새로 만든다 |
| `cmdUndo()` | 한 단계 되돌리기 (`undoStack`) |
| `setMode(m)` | `"free"` + 시나리오 6종(`"commit"` `"merge"` `"push"` `"pr"` `"clone"` `"rebase"`). 상태를 초기화한다 |
| `tourStart(step?)` / `tourGo(i)` / `tourJump(i)` | 안내 시작(0-based 단계 지정 가능) · 이웃 단계로 · 멀리 건너뛰기 |

- **거절 경로가 조용하다.** 가드에 걸리면 아무 반환값 없이 `#note`에만 문구를 쓴다 (`remoteGuard`, `cmdPush`의 non-fast-forward 등). 스크린샷 찍기 전에 `#note` 텍스트와 `S.log` 마지막 줄을 확인해야 "실행됐다고 착각한 상태"를 안 찍는다
- `setMode`는 `clone`이면 `freshClone()`, `commit`이면 `freshEmpty()`, 아니면 `fresh(m === "free" || m === "pr")`를 부른다. 즉 **원격을 갖고 시작하는 모드는 자유 모드와 pr·clone**이고, `push`는 **원격 없이 시작해 `git remote add`부터 직접 한다**. `remoteGuard`는 `S.branches[REMOTE]`가 아니라 **`S.origin`**을 본다 — 주소만 등록하고 아직 아무것도 안 올린 중간 상태가 있기 때문
- **`S.head`가 빈 채로 시작하는 모드가 둘이다** — `clone`(서버에만 저장소가 있다)과 `commit`(저장소 자체가 없고 커밋도 0개다). `cur()`가 `undefined`가 되므로 `render()`나 새 명령에서 `cur()`를 쓸 때 반드시 방어할 것 (`cur().head`에서 터졌던 자리다). 로그 머리말(`git init` 묶음)은 `clone`·`commit`·`push` 세 모드에서 숨긴다 — 그 줄들을 시나리오가 직접 실행하므로 위아래로 겹친다
- **흐름 띠(`#flowBar`)는 Commit 시나리오 전용이다.** 대합실 → 승강장 → 노선도 3칸으로 `add`·`commit`이 두 단계임을 보인다. 상태는 `flowUpdate()`가 정하고(`S.staged`면 승강장, 입력칸에 글이 있으면 대합실), 커밋 직후에는 `flowFly()`가 `flowHold`로 950ms 동안 노선도 칸을 붙잡는다 — **다음 동작이 오면 즉시 놓아준다.** 안 그러면 `add`를 눌러도 안 움직인다
  - **`flowUpdate()`는 `render()` 맨 앞에서 불러야 한다.** 띠가 뜨고 지는 것만으로 `.map-scroll`의 `offsetTop`이 101px 움직이는데, `placeZones()`보다 늦게 부르면 구역 머리글이 옛 자리에 남는다(62px 겹쳤다). 같은 이유로 **`ResizeObserver` 대상에 `#flowBar`가 들어 있다** — 노선도 위에 얹히는 것을 새로 추가하면 여기에도 넣을 것
  - **Commit 시나리오는 `add`와 `commit`을 절대 묶지 않는다**(8단계). 띠가 한 칸씩 움직이는 걸 매번 보게 하는 게 요점이다. 거절 단계(4단계)에서 띠가 **대합실에 머무는 것**이 "적기만 하고 담지 않았다"를 그대로 보여 준다
- **커밋이 0개면 노선도에 안내 문구 한 줄만 그린다** (`emptyMsg`, `git init` 전후로 문구가 바뀐다). 빈 화면이 고장으로 보이지 않게 하는 장치이고, 캔버스 최소 폭 340도 이때만 걸린다
- **빈 서버 띠**(`emptyBand` = `S.origin`은 있는데 원격 이름표가 없음)는 `remote add` 직후 상태다. 띠·머리글은 그리고 안에는 문구만 넣는다. `bandY`(이름표 세로 중심)는 이때 쓰이지 않고, 캔버스 최소 폭이 176 → 300으로 올라간다
- **시나리오를 추가하려면 네 곳을 손댄다** — `SCEN`에 항목, 헤더 `.modes .group` 안에 `<button data-mode="…">`, 원격을 쓴다면 `setMode`의 `fresh(...)` 조건, 그리고 안내 24·25단계 본문(시나리오를 이름으로 부른다). 버튼은 묶음 안에 있으므로 라벨에 "시나리오 ·"를 붙이지 않는다 — 390px에서 묶음이 7개까지 두 줄로 받는다
- **버튼 순서는 학습 순서다** (`Commit` → `Merge` → `Push` → `PR` → `Clone` → `Rebase`). `SCEN` 정의 순서와 다르며 **HTML 순서가 화면 순서**다. 앞의 둘은 혼자 하는 일, `Push`부터가 팀 작업, `Rebase`는 해시가 바뀌는 위험 개념이라 맨 끝이다. `Merge`와 `Rebase`는 같은 상황을 두 방식으로 처리하는 짝인데 양 끝으로 갈라져 있으므로, 안내 24단계와 README가 "맨 앞 Merge와 맨 끝 Rebase를 이어서 돌려 보라"고 짚어 준다 — **순서를 또 바꾸면 이 문구도 같이 고칠 것**
- **거절도 시나리오다.** `Commit`(담지 않고 커밋)과 `Push`(non-fast-forward)는 일부러 거절당하는 단계를 넣었다. 거절 경로는 `render()`를 불러야 빨간 로그가 **즉시** 보인다 — `cmdCommit`이 이걸 빠뜨리고 있었다(22차에 수정)
- **"이전 단계"는 `undoStack`이 아니라 `stepStack`을 쓴다.** `undoStack`은 명령이 `snap()`을 부를 때만 쌓이는데 **거절 경로는 `snap()`을 부르지 않는다**(`cmdCommit`·`cmdPush`의 거절). Commit·Push 시나리오가 일부러 거절 단계를 넣었으므로 `cmdUndo`를 그대로 쓰면 한 칸 어긋나 두 단계 전으로 튄다. `stepStack`은 `btnNext`가 **실행 직전** 상태를 통째로(`clone(S)` + `#note` + 입력칸) 담으므로 `snap()` 호출 횟수와 무관하다. **명령을 거꾸로 실행하지 말 것** — `tourJump`의 덧실행 함정과 같다
- **되돌릴 때 `seenNodes`/`seenEdges`는 사라진 id만 걷어낸다.** 통째로 비우면 다시 앞으로 갈 때 화면의 모든 역이 한꺼번에 등장 애니메이션을 다시 탄다. 그대로 두면 반대로 새 역이 애니메이션 없이 튀어나온다. `prevPos`는 비운다(없어진 역에서 날아오는 유령 이동 방지)
- 시나리오 단계(`SCEN[m].steps[].cmd`)는 문자열 또는 **배열**이다. 배열이면 한 단계가 여러 줄로 그려지고 한 번에 함께 체크된다(`git add .` + `git commit`). **진행 순서와 아래 로그는 줄이 1:1로 맞아야 한다** — `cmdCommit(msg, true)`는 담기까지 하므로 로그에 `git add .`이 먼저 찍힌다는 걸 잊기 쉽다
- `tourGo`는 **지나친 단계의 동작을 재생하지 않는다.** 멀리 이동할 때는 `tourJump`를 쓸 것. 주소에 `#step=N`을 붙여 열어도 같은 경로로 재개된다

### 렌더 파이프라인 (`render()`)

순서에 의존성이 있다. 중간에 끼워 넣을 때 주의.

1. `reach` — 모든 브랜치 head의 조상 집합. 여기 없으면 "버려진 커밋"
2. `depth` — 부모 최대 depth + 1 → 가로 칸 번호
3. `gotten` / `unfetched` — "내가 받은 커밋" 판정. **줄 배치보다 먼저** 구해야 한다(3번이 이걸 쓴다). `S.head`가 비면(clone 전) 통째로 비운다
4. `rowKey` / `rows` / `rowOf` — 세로줄 배치. `rowKind(id) + lane`으로 묶고 **L → G → R** 순으로 정렬해 행 번호를 부여. 빈 lane은 자동 압축된다
   - `L` 내 컴퓨터에 있는 역 / `G` rebase 후 버려진 역 / `R` **서버에만 있고 아직 안 받은 역**
   - **`R`을 맨 아래로 몰아야 "내 컴퓨터" 머리글 아래에 서버 것이 섞이지 않는다.** 버려진 역은 로컬에서 생긴 것이므로 `R`보다 위에 남는다
   - `firstRemoteRow`가 서버 구역의 시작 줄이다. `R` 줄의 역은 `pos`에서 `R_CAP`만큼 더 내려 "팀 공용 서버" 머리글이 역 메시지를 덮지 않게 한다
5. `labels`(로컬) / `rlabels`(원격) — 커밋별 이름표 묶음. `stackMax`, 동적 `ROW_H`. 원격 이름표는 `known`에 붙으므로 `known`을 비우면 서버 띠가 통째로 사라진다
6. `gapAt` / `colX` — 이름표가 다음 칸 역을 덮지 않도록 열 간격 확대. 서버 띠 이름표 묶음 폭(`bandW`)도 반영한다
7. `shift` — 서버 이름표 묶음이 `x=0` 밖으로 나가면 `colX` 전체를 오른쪽으로 민다
8. `pos` — 최종 좌표, `maxX` / `bandTop` / `bandY` / `maxY`. **`bandTop`은 고정이 아니다** — 서버 전용 줄이 있으면 그 줄 위로 올라와 역까지 구역에 담고, 없으면 예전처럼 본문 아래에 붙는다
9. 줄 안내선 → 서버 띠(배경·경계·연결 점선) → 선로(edge) → 역(node) → 이름표+HEAD → 서버 띠 이름표 순으로 그린다 (z-order가 이 순서에 의존)

### 화면 배치

```
┌ 노선도(범례·명령 띠·그래프) ┐ ┌ 조작판 ┐   ← .panel.map / .panel.console
└──────────────────────────┘ │ 현재위치 │
┌ 진행 순서 │ 실행한 명령 ──┐ │ 설명    │      .panel.work (두 행에 걸친 조작판 오른쪽)
└──────────────────────────┘ │ 버튼    │
                             └────────┘
```

- **진행 순서와 로그는 조작판이 아니라 노선도 아래에 둔다.** 한 열에 다 쌓으면 조작판만 862px가 되어 1600×1000에서 문서 높이가 1023px — 로그가 화면 밖으로 밀렸다. 지금은 전 시나리오에서 doc = 뷰포트 높이 안에 들어온다
- 둘을 **같은 패널에** 두는 이유: 진행 순서와 로그는 줄이 1:1로 맞아야 옮겨 칠 때 안 헷갈린다. 좌우로 떼면 그 대조가 깨진다. 자유 모드에서는 `#stepPanel`이 `display:none`이라 로그가 자리를 다 쓴다 (JS 분기 불필요)
- **1400px 이상**에서만 `.wrap` 상한을 1240 → 1500으로 올리고 조작판을 330 → 560으로 넓혀 명령·원격 두 묶음을 나란히 세운다. **상한을 같이 안 올리면 Rebase의 그래프(715px)가 안 들어가 가로 스크롤이 생긴다**
- `#freePanel`의 2열 미디어 쿼리는 **`#freePanel` 기본 규칙보다 뒤에** 놓아야 한다. 특정도가 같아 앞에 두면 덮인다
- 안내 말풍선은 노선도 패널 아래가 아니라 **밝힌 대상의 아래끝** 기준으로 붙고, 아래 자리가 모자라면 대상 위로 뒤집는다. 노선도 아래가 이제 비어 있지 않기 때문이다

구역 머리글(`#zoneLocal` / `#zoneRemote`)만 **SVG 밖 HTML 오버레이**다. 가로 스크롤을 따라가면 안 되기 때문. 위치는 `render()`에서 `svg.parentNode.offsetTop` 기준으로 계산한다 (SVG 요소엔 `offsetTop`이 없다)

### 좌표 규칙

| 상수 | 값 | 뜻 |
|---|---|---|
| `COL_W` | 132 | 기본 가로 칸 간격 |
| `ROW_H0` | 122 | 기본 세로줄 간격 (이름표가 쌓이면 늘어남) |
| `LBL_GAP` | 28 | 이름표 세로 간격 |
| `TOUR_DOCK` | 20 | 밝힌 대상과 안내 말풍선 사이 고정 여백 |
| `BAND_H` | 68 | 서버 띠 높이 (**서버 전용 역이 없을 때만** 쓰인다) |
| `BAND_GAP` | 12 | 노선도 본문과 서버 띠 사이 여백 |
| `BAND_CAP` | 27 | "내 컴퓨터" 머리글 자리 (원격이 있을 때만 `topPad`에 더해진다) |
| `R_CAP` | 31 | 서버 구역 줄을 더 내리는 폭 — "팀 공용 서버" 머리글 자리 |

- `ROW_H = max(122, (stackMax-1)*28 + 82)` — 쌓인 이름표가 아래 줄 글자와 겹치지 않게
- **글자 크기와 이 상수들은 한 묶음이다.** 본문 16px / 노선도 이름표·역 메시지 14px / 해시 13.5px가 기준이고, `LBL_FONT`(폭 실측용 문자열)도 **14px로 함께** 맞춰야 한다 — 어긋나면 알약이 글자를 못 담는다. 폰트를 또 건드리면 알약 높이(24)·흰 바탕(32)·HEAD 뱃지(56×26)·`lblW`의 여백(+24)까지 같이 움직여야 한다
- **Rebase 그래프가 781px까지 뻗는다.** 1400px 이상에서 조작판이 560px를 넘으면 노선도에 가로 스크롤이 생긴다 (620px로 넓혔다가 48px 넘쳤다). 조작판을 넓히고 싶으면 `.wrap` 상한(1560px)을 함께 올릴 것
- 커밋 원 위쪽에 **메시지 → 해시 → 원** 순으로 배치. 아래쪽은 비워 둔다
- 이름표는 원 오른쪽에 세로로 쌓이고, 현재 브랜치 이름표 오른쪽에 HEAD 뱃지가 붙는다

## 4. 알려진 함정 (반복 금지)

- **SVG `<g transform="translate()">`에 CSS `transform: scale()` 애니메이션을 직접 걸면 translate가 덮어써져 요소가 (0,0)으로 날아간다.** translate용 그룹과 scale 애니메이션용 그룹을 반드시 분리할 것 (`g` / `gi` 구조가 그 때문)
  - 같은 이유로 **이동 애니메이션은 바깥 `g`에만** 건다. CSS `transform`이 SVG `transform` 속성을 대신하므로 애니메이션이 끝나도 자리가 유지된다. 이전 자리를 넣고 **rAF 두 번 뒤** 새 자리로 바꿔야 전환이 걸린다 (한 번이면 같은 프레임으로 묶여 즉시 점프한다)
  - **선로(edge)는 따라 움직일 수 없다.** 양 끝이 서로 다른 역에 붙어 있어 하나의 transform으로 끌 수가 없다. 자리가 바뀐 역에 닿은 선로는 새 자리에서 `edge-fade`로 떠오르게 한다. 등장 애니메이션(`edge-enter`)과는 **둘 중 하나만** 걸 것
- `svg { min-width: 100% }`를 주면 viewBox 대비 늘어나며 요소가 잘린다
- 노드 위쪽에 텍스트, 아래쪽은 여백 — 이 배치를 유지. 위아래 여백 부족으로 원이 잘리지 않는지 매번 확인
- 사용자 입력(커밋 메시지, 브랜치 이름)을 `innerHTML`에 넣기 전 **반드시 `esc()`** 를 통과시킬 것
- **`tLater`로 예약한 동작은 다음 `tClear`에 지워진다.** 안내 단계를 건너뛸 때 그 단계의 두 번째 동작이 통째로 빠지는 원인이었다. `tJumping` 플래그가 켜져 있으면 즉시 실행된다 — 새 지연 동작을 넣을 때 이 경로를 확인할 것
- **rebase로 버려지는 커밋은 새 lane으로 옮긴다** (`ghostLane`). 안 그러면 앞선 rebase의 유령과 같은 lane·같은 칸에 놓여 서로 가린다
- **CSS 그리드의 `1fr`은 `minmax(auto,1fr)`이다.** `white-space:nowrap` 버튼이 든 트랙은 콘텐츠 최소폭만큼 부풀어 컨테이너를 뚫고 나간다 — 조작판 두 열이 `356px 248px`로 벌어져 패널 밖으로 30px 삐져나갔다. 글자를 키우면 바로 터지므로 **`minmax(0,1fr)`로 못박을 것**
- **구역 머리글 위치는 `render()` 안에서만 잡으면 안 된다.** `.map-scroll`의 `offsetTop`은 범례가 몇 줄로 접히느냐에 따라 달라지는데, 웹폰트가 적용되며 줄 수가 바뀌는 리플로우는 그 프레임에 아직 반영되지 않는다. 첫 렌더 값(19px 어긋남)이 그대로 남아 머리글이 `origin/main` 이름표를 덮었다. 좌표 계산은 `placeZones()`로 떼어 두고 **범례에 `ResizeObserver`를 걸어** 다시 잡는다
- **첫 줄 `<!DOCTYPE html>`을 지우지 말 것.** 없으면 quirks mode로 떨어져 `document.scrollingElement`가 `BODY`가 되고 **window `scroll` 이벤트가 안 잡힌다** (안내 스포트라이트가 스크롤을 못 따라갔다)
- **이름표 폭을 글자 수로 어림잡지 말 것.** Pretendard는 고정폭이 아니라 한글 10자부터 알약을 넘쳤다. `textW()`로 실측한다. 웹폰트가 늦게 오면 첫 렌더가 대체 글꼴 기준이므로 `document.fonts.ready`에서 캐시를 비우고 다시 그린다
- **`tourJump`는 반드시 `tBase`(안내 시작 상태)로 되돌린 뒤 재생한다.** 현재 상태 위에 덧실행하면 같은 커밋이 두 번 붙는다
- Bash 도구에서 PowerShell here-string(`@'...'@`)은 동작하지 않는다. 여러 줄 커밋 메시지는 heredoc(`<<'EOF'`)을 쓸 것

---

## 5. 작업 이력

원본 3개 버그부터 서버 띠·fetch·안내 장 구성까지 전체 변경 내역은 `HISTORY.md`에 있다.
지금 지켜야 할 규칙은 위 1·4절에 다 옮겨 두었으므로, 이력은 "왜 이렇게 됐는지"를 물을 때만 열면 된다.

---

## 6. 브랜치 운영

| 브랜치 | 역할 |
|---|---|
| `main` | 배포본. GitHub Pages가 서빙한다. **머지 = 배포** |
| `develop` | 작업용 |

```bash
git switch develop
# 수정 & 커밋
git switch main
git merge develop
git push
```

두 브랜치는 **같은 파일 목록**을 유지한다. 브랜치별로 파일을 나누면 머지 때마다 충돌한다.

Pages는 브랜치 하나만 서빙하므로 develop의 변경은 배포에 반영되지 않는다. 작업 중 확인은 로컬 서버로.

## 7. 미결 / 판단 보류

- **팀원은 `origin/main`에만 커밋한다** (`cmdTeamCommit`). 기능 브랜치가 서버에서 갈라지는 상황은 안 다룬다 — 경우의 수가 늘고 수업 한 타임에 담기 어렵다
- **원격 브랜치 삭제·`--prune`는 없다.**
- **PR은 리뷰·코멘트·승인 UI 없이 "병합 결과"만 만든다** (`cmdPR`). 그건 GitHub 화면이지 저장소 상태가 아니다. 대신 **PR 열기 단계**(`cmdPROpen`)를 두어 "그 구간은 저장소를 안 바꾼다"는 것을 말로 설명한다 — 생략처럼 보이던 3→4단계 사이를 메우는 장치다. `git branch -a`·`git remote add`와 같은 **"아무것도 안 변하는 단계"** 패턴이다
- **`push`가 PR을 만드는 게 아니다.** PR은 git 기능이 아니라 GitHub 기능이고, push한 본인이 웹에서 따로 만든다. push는 전제일 뿐(서버에 브랜치가 있어야 가리킬 수 있다). 학생이 가장 자주 오해하는 지점이라 4단계 설명이 **부정문으로 시작한다**. GitHub 버튼 이름은 바뀔 수 있으므로 문구가 특정 라벨에 기대지 않게 둘 것

**해결된 항목** — 원격 다중 이름표 레이아웃(`ccdade2`), rebase 반복 시 유령 겹침(`ghostLane`), 파비콘(인라인 data URI), `git fetch` 표현(`057475b`), PR 병합(`85c09ce`), 범례 아이콘 구분(버려진 커밋=점 / 아직 안 받은 역=파선), 안내 자동 넘김·9단계 두 번째 커밋 실기 확인 완료, clone·원격 추적 브랜치(18차 — `시나리오 · Clone`)

## 8. 기호 규칙

버려진 커밋과 아직 받지 않은 역은 **색과 선 종류를 함께** 다르게 둔다. 색 하나로만 구분하면 학생이 못 알아본다.

| 상태 | 색 | 역(원) | 선로(edge) |
|---|---|---|---|
| 살아있는 커밋 | 브랜치 색 | 실선, `opacity 1` | 실선, 굵기 7 |
| 버려진 커밋 (rebase 후) | `#AEB8C4` | **점** `0.1 5` + round cap, `opacity .45` | **점** `0.1 7`, 굵기 3, `opacity .4` |
| 아직 받지 않은 역 | `REMOTE_COLOR` | **파선** `7 5`, `opacity .8` | **파선** `7 5`, 굵기 5, `opacity .7` |

범례 아이콘(`.lg i.ghost` = `dotted`, `.lg i.unfetched` = `dashed`)도 이 규칙을 따라간다. 한쪽만 바꾸면 그림과 범례가 어긋난다.

**이름표 알약 글씨색은 고정값이 아니라 `pillText(color)`로 고른다.** 노선 색 위 흰 글씨는 밝은 색에서 대비가 무너진다(주황 2.77 · 하늘 2.82 · 초록 3.13 · 보라 4.12, 기준 4.5). 노선 색은 은유의 핵심이라 그대로 두고 글씨만 검정/흰색 중 대비가 큰 쪽을 쓴다 — 실제 지하철 노선도도 밝은 노선에는 검은 글씨를 쓴다. `COLORS`에 색을 추가해도 자동으로 걸린다. 현재 6색 전부 4.84 이상.

같은 이유로 **흐린 회색 `#8A96A4`·`#9AA5B1`은 본문 글자에 쓰지 않는다.** 로그의 `git init` 묶음(대비 2.63)과 「내 컴퓨터」 구역 머리글(3.01)이 기준 미달이었다. 흐리게 구분해야 할 때는 `var(--ink-soft)`(4.99~5.7)까지만 내리고, 진하기 차이는 옆의 `var(--ink)` 본문이 만들어 준다.

**범례는 아이콘 + 이름만 둔다.** 노선도 **위**에 있고(아래에 두면 SVG 높이가 바뀔 때마다 따라 움직인다) 꼬리 설명은 붙이지 않는다 — 1440px 한 줄, 390px 두 줄로 유지되는 게 자리 고정의 전제다. 기호의 뜻(점=rebase 후 버려짐, 파선=아직 fetch 전)은 **안내 3부 "기호 읽는 법"**([index.html](index.html)의 `sel:".legend"` 단계)이 맡는다. 양쪽에 같은 설명을 두지 말 것.

범례 **바로 아래**, 노선도 **바로 위**에 **방금 실행한 명령**(`.cmdbar` / `#curCmd`)이 자기 줄로 놓인다. 사이드바 맨 아래 로그는 그림에서 대각선으로 가장 먼 자리라 "무엇을 쳐서 무엇이 달라졌는지"가 안 붙어 보였다.

- **자기 줄이어야 한다.** 남는 자리를 쓰는 배치는 전부 실패했다 — 그래프 오른쪽 빈 공간은 Merge 169px·Rebase 123px까지 좁아지고 `.map-scroll` 안에 넣으면 가로 스크롤을 따라 흘러간다. 범례 줄 끝에 얹었더니 남는 폭이 1200px 창에서 249px, 950px에서 0까지 줄어 잘렸다(가장 긴 `git remote add origin …`는 420px가 필요하다)
- 출처는 `S.log`의 **마지막 non-`"o"` 줄**이다. `"o"`는 명령이 뿌린 출력이라 건너뛰고 그 앞 명령을 잡는다(`git branch -a`). 색·`$` 접두는 로그의 `c`/`e`/`n` 규칙을 그대로 쓴다 — 자유 모드에서도 동작한다
- **거절(`e`)은 배경까지 칠한다**(`#FCEBEC`). 뒷자리에서 "막혔다"가 즉시 읽히는 게 이 배치의 최대 이득이다. Commit·Push 시나리오가 일부러 거절을 넣은 이유를 화면이 받쳐 준다
- **명령이 없어도 높이(36px)를 예약한다**(`.cmdbar.empty`). 접었다 펴면 첫 실행에서 노선도가 툭 내려간다
- ellipsis는 `display:block` + `nowrap` + `overflow:hidden`에서만 먹는다. flex 컨테이너로 만들면 안 걸린다
- 900px 이하는 숨긴다. 한 열로 접히면 사이드바가 노선도 바로 아래로 와서 이미 가깝다
