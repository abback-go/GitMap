# 작업 이력

`CLAUDE.md`에서 분리한 변경 내역. 지침이 아니라 기록이다.
현재 유효한 규칙·함정은 `CLAUDE.md` 쪽에 있다.

원본은 HEAD 표시·이름표 겹침·모바일 미검증 3개 버그를 가진 상태였다. 아래는 그 이후 전체 변경 내역.

**오래된 것이 위, 최신이 맨 아래다.** 새 항목은 파일 끝에 붙인다.

## 1차 — 최초 3개 버그

**A. HEAD가 어느 브랜치를 가리키는지 안 보임 (최우선)**
`git switch -c` 직후 이름표만 두 개 붙고 HEAD는 커밋 아래 그대로 있어, 안내 문구("HEAD가 새 노선으로 옮겨갔다")와 그림이 어긋났다.

- 커밋 아래 HEAD 뱃지를 없애고 **HEAD → 브랜치 이름표 → 커밋** 3단 구조로 변경
- 이름표를 커밋별로 묶어(`labels`) 오른쪽에 세로로 쌓고 연결선으로 커밋과 연결
- HEAD 뱃지는 현재 브랜치 이름표 오른쪽에 붙고 화살표가 그 이름표를 가리킨다
- 현재 브랜치 = 진한 테두리 + `font-weight:800` + 불투명 / 나머지 = `opacity:.5`

**B. 브랜치 이름표 겹침**

- `ROW_H`를 상수에서 계산값으로 (쌓인 개수에 따라 확대)
- 열 간격 동적 확대 (`gapAt`)
- 이름표 뒤 흰 바탕 rect — 선로 위에 얹혀도 읽힘
- `maxX`/`maxY`가 이름표+HEAD 폭까지 포함하도록 수정
- 커밋 없는 lane의 안내선은 그리지 않음

**C. 모바일 미검증**

- `tourPlace()`: 대상이 화면 밖이면 `scrollIntoView`, 말풍선은 여백 넓은 쪽에, 부족하면 `scrollBy`로 자리 확보 (기존엔 스크롤을 안 해서 390px에서 조작 패널 단계 스포트라이트가 화면 밖이었다)
- 노선도 가로 자동 스크롤: 기본은 최신 역, HEAD가 왼쪽으로 잘리면 HEAD 쪽으로

## 2차 — rebase 유령 커밋 + 리뷰 발견분

- **rebase 후 버려진 커밋이 새 커밋에 가려지던 문제** → `rowKey`로 살아있는 줄/버려진 줄을 분리. 부수 효과로 빈 lane 압축도 됨
- 자동 안내 2단계 문구 오류 정정 (해시/메시지 위아래 순서가 실제와 반대였다)
- **안내를 닫아도 예약된 단계 동작이 실행되던 버그** — `clearTimeout(tTimer)`가 죽은 코드였다. `tLater`/`tClear`로 예약 타이머를 모아 관리
- **XSS** — 커밋 메시지·브랜치 이름이 `innerHTML`로 들어갔다. `esc()` 추가
- 빈 커밋 메시지로 커밋하면 로그에 `git commit -m ""`이 남던 문제
- 브랜치 이름 검증 추가 (git `check-ref-format` 핵심 규칙, 한글은 실제 git처럼 허용)
- 현재 브랜치로 `이동` 시 무반응 → 안내 문구
- `▶ 자동 안내` 버튼에 `aria-pressed`가 붙던 문제
- 범례에 HEAD 항목 추가

## 3차 — rebase 정확성 + 안내 재실행

- **rebase가 병합 커밋을 복제하던 문제** → 실제 git처럼 병합 커밋은 replay에서 제외 (`--rebase-merges` 없을 때의 동작). 버려진 병합 커밋은 자동으로 점선 줄로 내려간다
- 안내에서 뒤로 갔다 앞으로 오면 단계 동작이 중복 실행되던 문제
- 버려진 병합 커밋의 가운데 점만 원색으로 남던 시각 결함

## 4차 — 안내 재생 제어

- **이전 버튼을 눌러도 노선도가 되감기지 않던 문제** → 단계 진입 시점의 상태를 `tSnap[]`에 스냅샷하고 복원. 동작이 있는 단계는 재실행되므로 이후 스냅샷을 무효화. 이 구조 덕분에 3차의 중복 실행 방지 플래그(`tRan`)는 제거됨
- **일시정지해도 단계 시계가 계속 흘러, 이어보기를 누르면 바로 다음 단계로 넘어가던 문제** → `tT0`/`tPauseAt`로 멈춘 시간만큼 시작 시각을 미룸

## 5차 — 안내 말풍선 위치

- 여백 넓은 쪽으로 위아래를 오가던 말풍선을 **노선도 패널 바로 아래에 고정 여백 20px**로 고정 (넓은 화면)
- 가로 중앙도 화면이 아니라 **노선도 폭 기준** — 화면 중앙에 두면 오른쪽 조작판을 30px 물었다
- 좁은 화면(<900px)은 붙일 빈 자리가 없어 화면 아래 22px 고정 + 페이지 여백 확보 방식 유지
- 뷰포트가 낮으면 여백이 줄어들 뿐 말풍선이 화면 밖으로 나가지 않도록 클램프

## 6차 — 10단계 서사 불일치

안내 10단계가 "팀원이 main에 커밋했다"고 설명하면서 실제로는 내가 main에서 커밋하고 있었다. 로그에는 `git switch main` + `git commit`이 남는데 19단계에서 "이 로그를 그대로 터미널에 치면 같은 결과"라고 말하는 자료라 그냥 둘 수 없었다.

1차로 문구를 "`git pull`로 받아오지만 여기서는 흉내 낸다"로 고치고 관찰 단계를 추가했으나, 사용자 지적으로 **실제 원격 저장소를 구현**하는 방향으로 확대.

## 7차 — 원격 저장소 구현

- `origin/main`을 `remote:true` 브랜치 항목으로 추가. 추적 대상은 main 하나 (실제 git도 새 로컬 브랜치는 push 전까지 원격에 없다)
- 선로 색은 main과 동일 ("노선 = 색" 규칙 유지), **속이 빈 이름표**로만 원격을 구분
- 자유 모드 버튼 3개: `팀원이 커밋` / `받아오기 · pull` / `올리기 · push`
  - pull: 뒤처졌으면 fast-forward, 갈라졌으면 병합 커밋 (`git pull` = fetch + merge)
  - push: 원격이 조상이면 전진, 아니면 **거절**(non-fast-forward, 로그에 빨강)
  - 가드: main이 아닌 브랜치, 이미 최신, 올릴 것 없음, 내 쪽이 앞섬
- 시나리오 모드(Merge/Rebase)는 원격을 만들지 않는다 (`fresh(withRemote)`)
- **렌더러 버그 2건 수정** (이 작업에서 드러남)
  - 원격이 앞선 상태에서 로컬 커밋 시 두 커밋이 같은 칸에 겹치던 문제 → `doCommit`에 갈라짐 규칙 추가 (같은 줄에 이미 선로가 뻗어 있으면 새 줄로, 색은 유지)
  - 열 간격 계산이 "부모-자식인 다음 역"만 봐서, rebase 후 `origin/main`이 무관한 다음 역과 겹치던 문제 → **같은 줄 다음 칸이 차 있는지**로 판정하도록 교체
- 안내 21단계로 재구성. **초반 push 단계가 없으면 origin이 뒤처져 12단계 pull이 fast-forward가 아니라 병합이 되어 버린다**는 문제를 발견해 6단계에 push를 넣어 해결

```
4 커밋 → 5 관찰 → 6 원격에 올려두기(push) → 7 브랜치 → 8 관찰 → 9 기능 커밋 2회
→ 10 main으로 이동 → 11 팀원이 원격에 커밋 → 12 원격만 앞서 나감(관찰)
→ 13 받아오기(pull, fast-forward) → 14 두 노선 갈라짐(관찰) → 15 병합 → 16 환승역(관찰)
→ 17 올려서 공유(push) → 18 기호 → 19 로그 → 20 시나리오 → 21 마무리
```

결과적으로 로그가 그대로 재현 가능한 협업 세션이 되었고, "흉내 냅니다" 류의 면피 문구가 사라졌다.

## 8차 — 저장소 구성

- `git init` → GitHub `abback-go/GitMap`(PUBLIC, 비어 있던 상태)에 푸시
- README 작성 → 이후 **학생용으로 재작성** (맨 위 바로 열기 링크, 자동 안내부터 시작하는 순서, 화면·기호 읽는 법. 개발 내용은 `<details>` 접이식)
- GitHub Pages 활성화 (`main` 브랜치 root)
- `git-branch-map.html` → **`index.html`로 이름 변경** — Pages 루트에서 바로 열리도록. 학생 링크가 짧아졌다
- `develop` 브랜치 생성

## 9차 — 문서 정리

- `CLAUDE.md`에 표준 헤더 추가, 작업 이력을 이 파일로 분리
- 검증 절차를 Playwright 전용에서 브라우저 MCP 일반(Playwright / claude-in-chrome)으로 고쳐 씀
- 전역 조작 함수 표를 `CLAUDE.md` 3절에 추가
- 루트에 남아 있던 `before-desktop.png`를 `shots/`로 이동

## 10차 — 기능 브랜치도 서버에

- 서버 띠에 `origin/main` 하나만 보여서 "브랜치는 내 컴퓨터에만 있는 것"이라는 오해를 남겼다
- `RPFX` 접두사 기반으로 원격을 일반화. `cmdPush`가 원격 없는 브랜치를 만나면 `git push -u origin <이름>`으로 새로 만든다
- 선행 조건이던 레이아웃 버그 2건 수정 — 같은 역의 이름표 묶음 가운데 정렬, 좌우 끝을 `maxX`·`gapAt`에 반영, 왼쪽 넘침은 `colX` 전체 이동으로 보정
- `remoteGuard`의 main 전용 제약 제거

## 11차 — 안내 3장 구성

2타임 연속 강의용. 안내를 쪼개지 않고 장 표시만 붙였다.

- `ch` 필드는 장 시작 단계에만. 나머지는 `TOUR_CH`가 이어받으므로 단계를 넣고 빼도 경계가 안 어긋난다
- 진행 막대를 단계당 한 칸으로 쪼개고 장 경계에 틈. 칸을 누르면 `tourJump`
- `tourJump`는 지나친 단계의 동작을 재생하고 스냅샷도 채워 되감기를 유지한다
- **`tLater` 예약이 건너뛰기에서 지워지던 버그** — 9단계의 두 번째 커밋("이단 점프")이 통째로 빠졌다. `tJumping` 플래그로 즉시 실행
- `#step=N` 해시로 재개. `localStorage` 금지라 주소에 남긴다
- 쉬는 시간 경계는 11 → 12단계

## 12차 — 미결 정리

- **rebase를 두 번 하면 유령 커밋이 겹치던 문제 재현·수정.** 검증해 보니 실제로 두 역이 같은 좌표(290,296)에 놓여 하나가 가려졌다. `rowKey`가 lane으로만 묶는데 두 세대의 유령이 같은 lane을 쓰기 때문. rebase 때 버려지는 커밋을 새 lane(`ghostLane`)으로 옮겨 세대별로 줄을 나눴다. 수정 후 3줄 → 4줄, 중복 좌표 0
- **파비콘 추가.** 인라인 SVG data URI라 단일 HTML 제약을 유지한다. 갈라지는 두 노선과 환승역 모양
- CLAUDE.md 3·4·7절을 현재 코드에 맞게 갱신, README를 24단계·서버 띠·fetch 기준으로 수정

## 13차 — Codex 최종 검수 반영

리뷰에서 Major 3건 + Minor 1건. 전부 브라우저에서 재현 확인 후 수정.

- **진행 막대로 앞 단계를 누르면 커밋이 중복 생성**됐다. `tourJump`가 현재 상태 위에 이전 단계 동작을 덧실행했기 때문. 재현: 4단계 실행 → 5단계 → 막대에서 4단계 클릭 → `적 AI 추가`가 두 개. `tBase`(안내 시작 스냅샷)를 저장하고 건너뛸 때 먼저 복원하도록 수정
- **스크롤하면 스포트라이트가 안 따라왔다.** 파고들어 보니 원인은 `resize`만 듣던 게 아니라 **DOCTYPE 누락으로 quirks mode**였던 것 — `scrollingElement`가 `BODY`가 되어 window `scroll` 이벤트가 아예 안 잡혔다. `<!DOCTYPE html>` 추가 + `scroll` 리스너 연결(`tourSync`는 스크롤을 다시 건드리지 않고 자리만 맞춘다). 표준 모드 전환 후 1440px·390px 레이아웃 회귀 없음 확인
- **한글 브랜치 이름이 알약을 넘쳤다.** 폭을 `글자수 × 7.6`으로 어림잡았는데 Pretendard가 고정폭이 아니다. `기능개발브랜치입니다`가 실제 99.4px인데 알약은 96px. canvas `measureText`로 실측하도록 교체, 웹폰트 로드 후 재측정
- README에서 서버 띠 설명이 반대로 읽히던 것 수정 (띠 이름표는 서버의 현재 위치가 아니라 내가 마지막 fetch로 확인한 위치)

## 14차 — 스테이징(git add) 최소 모델

로그에 `git switch -c dev` → `git commit`이 연달아 남는데, 실제 git은
`git add` 없이는 커밋되지 않는다. 22단계의 "이 목록을 그대로 터미널에
치면 같은 결과"라는 약속이 깨져 있었다.

`git add .` 한 줄을 자동으로 끼워 넣는 대신, 사용자가 실제로 담는
동작을 하게 했다. 그래야 로그가 "내가 누른 것"의 기록으로 남는다.

- `S.staged` 추가. 입력칸 내용 = 내가 한 작업, `담기 · add`로 담는다
- 담지 않고 커밋하면 **거절**하고 로그에 빨강으로 남긴다
  (`git commit -m "..."   # nothing added`). 초보자가 가장 많이 겪는
  실패를 그대로 겪게 하는 게 이 모델의 목적이다
- 커밋하면 담긴 것이 비워지고 역이 생긴다
- 시나리오·자동 안내는 `cmdCommit(msg, true)`로 담기까지 자동. 24단계
  서사와 진행 순서 목록은 그대로 두되 **로그에는 `git add .`가 남는다**
- 스테이징은 조작 패널 안에서만 표현한다. 노선도에는 넣지 않았다 —
  역이 아닌 것을 지하철 은유에 넣으면 그림이 무너진다

이어서 `git init` 이야기가 나왔는데, 넣는다면 `git remote add origin`도 같이
넣어야 앞뒤가 맞는다(원격 설정 없이는 push 줄이 여전히 거짓말이다). 빈 저장소
모드를 만드는 대신 **로그 맨 위 고정 머리말**로 처리했다.

- `# 처음 한 번만 — 저장소를 만들 때` 아래 `git init` / `git remote add origin`
  두 줄을 흐리게 고정 표시하고 점선으로 아래 기록과 구분
- 전사가 완전해지고, "이건 매번 하는 게 아니다"를 형태로 가르친다
- 앱 상태·노선도·안내 구조는 안 건드렸다. init은 평생 한 번이라
  버튼과 빈 화면을 만들 값어치가 없다고 판단

## 15차 — PR 병합

7차에서 "PR은 git이 아니라 GitHub 기능"이라며 제외했는데, 서버 띠와
기능 브랜치 push가 생기면서 판단을 뒤집었다. PR은 **다른 어떤 기능도
못 가르치는 것**을 가르친다 — 병합이 내 컴퓨터가 아니라 서버에서
일어나고, 나는 그 결과를 받아와야 한다는 것. 그전까지 서버 띠에서
자발적으로 일어나는 일은 하나도 없었다.

- `cmdPR()` — 서버에서 `origin/<기능>`을 `origin/main`에 병합해 환승역을
  만든다. 로컬 브랜치도 HEAD도 안 움직이고, `known`도 그대로라 새 역이
  "아직 받지 않은 역"으로 흐리게 뜬다. 이어서 fetch/pull이 필요해진다
- 로그는 `# GitHub에서 Pull Request를 병합했습니다` 설명 줄. git 명령이
  아니므로 "복붙하면 재현된다" 약속이 안 깨진다
- 가드: 원격 없음 / 서버에 기능 브랜치 없음 / 이미 합쳐짐
- 리뷰·코멘트·승인 UI는 안 만든다. GitHub 화면이지 저장소 상태가 아니다

**안내 24 → 25단계.** PR과 로컬 merge는 같은 브랜치에 둘 다 못 쓴다(먼저 합친
쪽이 이기고 나머지는 "이미 합쳐졌다"로 거절된다). 그래서 안내 본선은 팀 협업
흐름인 PR로 가고, 로컬 `git merge`는 시나리오 모드 두 개가 전담하도록 나눴다.

```
17 두 노선 갈라짐(관찰) → 18 PR로 합치기 → 19 합쳐진 곳은 서버입니다(관찰)
→ 20 합친 결과 받아오기(pull) → 21 환승역이 생겼습니다(관찰)
```

빠진 것: 기존 18 `병합하기`(로컬 merge), 기존 20 `올려서 공유`(push — PR 뒤에는
서버가 이미 앞서 있어 올릴 게 없다). 17단계와 24단계 문구에 "합치는 길은 둘"과
"시나리오에서 로컬 방식을 본다"를 넣어 연결했다.

작업 중 발견해 함께 고친 것 2건:

- `.staged{display:flex}`가 `[hidden]` 기본값을 덮어써 **담긴 변경 칩이
  항상 보였다.** `.staged[hidden]{display:none}` 추가
- 아직 받지 않은 병합 커밋의 **가운데 점만 원색**으로 남았다(3차에서
  버려진 커밋에 대해 고쳤던 것과 같은 결함). 바깥 원과 같은 상태로 그린다

## 16차 — 안내 PR 단계 + 범례 아이콘 구분

- 안내 24 → 25단계. PR과 로컬 merge는 같은 브랜치에 둘 다 못 쓰므로(먼저 합친
  쪽이 이긴다) 안내 본선은 PR, 로컬 merge는 시나리오 모드가 전담하도록 나눴다
- **범례에서 `버려진 커밋`과 `아직 받지 않은 역`이 둘 다 점선 원**이라 색으로만
  구분되던 문제(7절 미결). 선 종류를 갈랐다 — 버려진 커밋은 **점**
  (`0.1 5` + round cap), 아직 안 받은 역은 **파선**(`7 5`). 지도와 범례를 함께
  바꿨다. 한쪽만 바꾸면 어긋난다
- `CLAUDE.md`에 8절 기호 규칙 표 추가, 낡은 단계 번호(21 → 22) 정정

## 17차 — Codex 2차 검수 반영

Critical 없음. Major 2 + Minor 3, 전부 확인 후 수정.

- **로그 재현 약속이 여전히 안 지켜졌다.** 머리말에 `git init` / `git remote add`만
  넣었는데, ① 최초 커밋 기록이 없고 ② upstream 설정이 없어 첫 `git push`가
  `no upstream branch`로 실패한다. 머리말을 다섯 줄로 늘려
  (`init` → `add` → `commit "최초 커밋"` → `remote add` → `push -u origin main`)
  해결. 팀원 커밋·PR은 `#` 주석이라 애초에 명령이 아니므로, 문구를
  "`$`로 시작하는 줄을 옮겨 치면 된다 / `#`은 팀원이나 GitHub이 한 일"로 갈랐다
- **원격 기능 브랜치가 둘 이상이면 PR이 삽입 순서상 첫 번째를 골랐다.** 사용자가
  고를 방법이 없었다. `selPR` 드롭다운 추가(아직 안 합쳐진 원격 브랜치만 채운다),
  `cmdPR(name)`으로 명시 전달. 이미 합쳐진 이름을 지정하면 거절
- 안내 되감기가 `S`만 복원하고 입력칸은 그대로였다 → `tourCapture`/`tourRestore`에
  `#msg`, `#bname` 포함
- 담긴 변경 칩에 `role="status"` `aria-live="polite"`
- 8절 기호 규칙 수치가 코드와 달랐다(선로 `0.1 7`, opacity가 역/선로/범례 제각각).
  표를 역·선로로 나눠 실제 값으로 고치고 범례 opacity를 `.85` → `.8`로 통일

## 18차 — 시나리오 · Clone (다른 PC에서 이어받기)

지금까지는 **빈 저장소에서 시작해 만들어 올리는** 방향만 다뤘다. 정작 학생이 처음
겪는 건 반대쪽 — **이미 있는 저장소를 clone해서 참여하는** 상황이다. 그 조각을 채웠다.

- 시나리오 5단계: `git clone` → `git branch -a` → `git switch develop` →
  `git commit` → `git push`
- **3단계가 핵심.** clone하면 로컬 브랜치는 기본 브랜치 하나뿐이라
  `origin/develop`은 보이는데 `develop`이 없다. `cmdSwitch`에 **로컬에 없고 원격에만
  있는 이름** 경로를 추가해, git이 추적 브랜치를 자동으로 만들어 주는 걸 보여준다.
  기존 `cmdSwitch`는 `if(!S.branches[name]) return;`으로 **조용히 막고 있었다**
- `freshClone()` — 서버에만 저장소가 있고 로컬은 빈 상태. `S.head = ""`가
  "아직 내 저장소가 없다"는 뜻이다. 서버는 `origin/main` 2커밋 + 거기서 갈라진
  `origin/develop` 1커밋
- 새 명령 `cmdClone()`(원격 전체 복사 + 기본 브랜치 생성), `cmdBranchList()`
  (상태를 안 바꾸는 조회. 서버에만 있는 노선을 눈으로 확인시킨다)
- **빈 로컬에서 `render()`가 터지던 곳 2군데.** `cur()`가 `undefined`가 되어
  `cur().head`에서 죽었다 → `c0 && c0.head`로 방어. `nowBranch`는 "아직 없음" 표시
- **`git init` 머리말이 clone 시나리오에서는 틀린 말이다.** 이미 있는 저장소를
  받아오는 이야기라 1단계 `git clone` 자체가 "처음 한 번만" 하는 일이다.
  `mode === "clone"`이면 머리말을 숨긴다
- `scenario.intro` — 시작 화면이 여느 시나리오와 다를 때만 따로 설명
- `.modes`에 `flex-wrap:wrap` — 버튼이 5개가 되어 390px에서 넘쳤다. 두 줄로 감싼다

**clone 전인데 로컬에 선로가 그려져 있던 문제.** 처음엔 문구로 때웠는데
("위쪽에 이름표가 하나도 없는 게 증거입니다") 지적을 받았다. 맞는 지적이다 —
교육 자료에서 그림이 사실과 다르면 안 된다.

원인은 `gotten` 계산이었다. "내가 받은 커밋"을 `원격 known ∪ 로컬 head`의 조상으로
구하는데, clone 전에도 원격 `known`이 채워져 있어 전부 "받은 것"으로 판정됐다.
**`S.head`가 비어 있으면(= 내 저장소가 없으면) `gotten`을 통째로 비우도록** 고쳤다.
한 줄이다.

```js
if(S.head) Object.values(S.branches).forEach(...)
```

그러면 서버 커밋 전부가 기존 문법 그대로 **"아직 받지 않은 역"**(`REMOTE_COLOR` +
파선 `7 5`)으로 그려진다. `known`을 비우는 방법은 쓰지 않았다 — `rlabels`가 `known`에
붙으므로 서버 띠 이름표가 통째로 사라지고(`if(!at) return`), 2부의 `fetch`
개념(`known ≠ head`)도 깨진다.

덤으로 **clone의 효과가 눈에 보이게 됐다.** 파선이던 역 3개가 한 번에 진해진다.
`intro`와 `cmdClone`의 문구도 "파선 → 진해짐"을 가리키도록 고쳤다.

**"clone 한 번에 develop 역까지 진해지는 게 맞나"** — 맞다. 실제 git으로 확인했다.
임시 서버(`main` 2커밋 + `develop` 1커밋)를 만들어 clone한 뒤 **서버를 지우고**
조회했더니 `git log origin/develop`도, `git show origin/develop:c.txt`도 그대로
나왔다(`git fetch`만 실패). clone은 **모든 브랜치의 커밋과 파일을 전부** 받아온다.
안 따라오는 건 **로컬 브랜치(이름표)** 뿐이다.

다만 그 위화감이 이 시나리오의 교육 포인트라서, 1단계 문구가 짚고 넘어가도록 고쳤다 —
"커밋은 다 왔는데 브랜치는 안 따라왔습니다". 2단계 `git branch -a`로 이어진다.

**`git branch -a` 단계에서 그림이 안 바뀌어 고장난 것처럼 보였다.** 조회 명령이라
저장소를 안 바꾸는 게 맞지만, "다음 단계"를 눌렀는데 화면이 그대로면 학생은 멈춘
줄 안다. 실제 터미널이 그렇듯 **로그에 목록을 뿌리도록** 했다. 로그 종류에 `"o"`
(명령이 뿌린 출력)를 추가 — `$`도 `#`도 없는 흐린 줄이라 옮겨 칠 대상이 아님이
형태로 구분된다.

```
$ git branch -a
* main
  remotes/origin/main
  remotes/origin/develop
```

문구도 "**그림은 그대로입니다** — 조회만 하는 명령이라 저장소를 바꾸지 않습니다"로
시작하게 고쳤다. 안 바뀌는 게 정상임을 먼저 말해 준다.

**"`git switch`는 서버에 없어도 브랜치를 만드는 명령 아닌가"** — 아니다. git 2.52로
세 경우를 확인했다.

| 상황 | `git switch develop` |
|---|---|
| 로컬·원격 둘 다 없음 | `fatal: invalid reference: develop` — 거절 |
| 원격에만 있음 | `set up to track 'origin/develop'` + `Switched to a new branch` |
| 새로 만들기 | `-c`가 필요. 없으면 똑같이 거절 |

**진행 순서에 `git add .`이 빠져 있었다.** 커밋 단계가 전부 `cmdCommit(msg, true)`
(담기까지 한 번에)라 로그에는 `git add .`이 찍히는데 진행 순서에는 없어서, 두 칸이
어긋나 보였다. **clone만이 아니라 merge·rebase도 같은 불일치**를 갖고 있었다.

`steps[].cmd`가 **배열도 받도록** 했다. 배열이면 여러 줄로 그려지고 한 번의
"다음 단계"에 **함께 체크**된다. 실행은 그대로 한 번이다.

```js
{ cmd:['git add .','git commit -m "보스 패턴 추가"'], run:()=>cmdCommit("보스 패턴 추가", true) }
```

세 시나리오 모두 진행 순서와 로그가 1:1로 맞는 걸 확인했다(clone 6줄 / merge 11줄 /
rebase 14줄). rebase 마지막 줄만 로그에 `   # fast-forward`가 더 붙는데, 이건 명령이
아니라 결과 주석이고 bash에서도 주석이라 그대로 옮겨 쳐도 동작한다.

앱은 이미 두 명령을 구분하고 있었다 — **브랜치 + 이동** 버튼이 `git switch -c`,
**이동** 버튼이 `git switch`. 다만 3단계 문구가 "서버에도 없으면 어떻게 되는지"를
빼먹어 오해를 샀다. "서버에 있을 **때만**", "`git switch`는 브랜치를 **만드는** 명령이
아닙니다"를 넣었다. `cmdSwitch`의 **조용한 거절**(`if(!r) return;`)에도 실제 git 메시지를
띄우도록 했다 — UI로는 닿기 어려운 경로지만 조용히 아무 일도 안 일어나는 건 이 앱이
반복해서 겪은 함정이다.

검증: 1440px·390px 양쪽, 5단계 전 구간 거절 로그 0건, 기존 3개 모드 회귀 없음
(clone → 자유 모드 복귀 시 머리말·원격 정상 복구).

## 19차 — 안내에 clone 단계 (25 → 26단계)

18차에서 clone 시나리오를 만들고도 자동 안내에는 넣지 않아, 안내를 끝까지 본
학생이 그 존재를 모른 채 끝났다. 3부 끝(24단계 "시나리오로 비교하기" 다음,
마지막 "마음껏 실험하세요" 앞)에 한 단계를 넣었다.

기존 단계에 한 문장만 덧붙이는 안도 있었지만 — 재생 시간이 안 늘어난다 —
**단계를 온전히 하나 주는 쪽을 골랐다.** 설명이 또렷해지고, `#modes`를 가리키므로
스포트라이트가 `시나리오 · Clone` 버튼을 그대로 밝힌다.

그 대신 `CLAUDE.md` 7절의 **"자동 안내 전체 재생 3분, 수업에서 길면 줄여야 한다"
판단 보류 항목을 지웠다.** 단계를 늘리기로 한 이상 유지할 수 없는 우려다.
"clone 시나리오는 안내에 넣지 않았다" 항목도 함께 삭제.

- 진행 막대·단계 번호는 `TOUR.length` 기반이라 자동으로 26으로 맞는다
- README의 "25단계" → "26단계", 3부 설명 "시나리오 비교" → "시나리오 셋 소개"

검증: 26단계 전 구간 `tourJump` 순회 오류 0건, 콘솔 에러 없음, 1000px·390px 양쪽에서
말풍선이 화면 안(1000px 622px+256 / 390px 497px+326).

**측정 함정 하나.** 단계 전환 직후 `getBoundingClientRect()`를 읽었더니 말풍선이
화면 밖(`fits:false`)으로 나온 것처럼 보였다. 전환 중간값이었다 — 별도 호출로 다시
재니 `tip.style.top`이 622px로 화면 안이었다. `CLAUDE.md` 2절이 경고한 그대로다.
그리고 **`tourGo(i)`는 상대 이동이 아니라 절대 인덱스**를 받는다(`tourGo(1)`은
다음 단계가 아니라 2단계). 표의 "이웃 단계로"라는 설명 때문에 헷갈리기 쉽다.

## 20차 — 시나리오 · Push (push가 거절될 때)

시나리오 셋이 **혼자 작업**(Merge/Rebase)과 **받아오기**(Clone)를 덮고 있어서,
"올리려는데 막히는" 축이 비어 있었다. 초보가 팀 프로젝트에서 가장 먼저·가장 많이
겪는 사고다 — *"왜 갑자기 push가 안 되지?"*

```
git add . + git commit         내가 작업
# 팀원이 origin/main에 커밋     그 사이 서버가 앞서 나간다
git push                       ✗ rejected (non-fast-forward)
git pull                       합치기 — 환승역이 생긴다
git push                       ✓
```

**새 명령을 하나도 안 만들었다.** `cmdPush`의 non-fast-forward 거절, `cmdTeamCommit`,
`cmdPull`의 merge 경로가 전부 이미 있었다. 시나리오 정의와 버튼만 추가했다.

걸린 곳은 하나. 시나리오 모드는 `fresh(m === "free")`라 **원격이 없었다.**
`fresh(m === "free" || m === "push")`로 고쳤다. 원격을 쓰는 시나리오를 새로 만들 때
이 조건을 빠뜨리면 `remoteGuard`에 걸려 **조용히 아무 일도 안 일어난다** — CLAUDE.md
3절에 적어 뒀다.

3단계 화면이 이 시나리오의 핵심이다. 내 `main`은 오른쪽으로, 팀원 커밋은 **파선 흐린
역**으로 아래에 갈라져 있고 `origin/main` 이름표는 제자리다. 갈라진 그림이 곧 거절
사유다.

- 진행 순서 2단계는 명령이 아니라 `# 팀원이 원격에 "UI 폰트 교체" 커밋을 올렸습니다`
  — 로그의 `"n"` 줄과 **문자열을 똑같이** 맞춰 1:1 대응을 유지했다
- 네 시나리오 전부 진행 순서 ↔ 로그 대조 통과 (clone 6 / merge 11 / rebase 14 / push 6)

**버튼이 여섯 개가 되어 390px에서 세 줄이 됐다**(113px). 넘침은 없지만 마지막 줄에
`시나리오 · Push` 하나만 남는다. 라벨을 줄이면 해결되지만 "시나리오 · " 접두어는
디자인 규칙(1절 4항)이라 손대지 않았다. **시나리오는 여기까지가 상한**으로 본다.

## 21차 — 시나리오 버튼을 묶음으로

20차에서 버튼이 여섯 개가 되며 390px에서 **세 줄**이 됐다. 시나리오를 더 넣을 수 없는
상태였다. 원인은 라벨이었다 — `시나리오 · Merge`, `시나리오 · Rebase`… 접두어를
버튼마다 반복하느라 폭의 절반을 쓰고 있었다.

접두어를 **묶음 레이블로 한 번만** 쓰도록 바꿨다.

```html
<span class="group" role="group" aria-label="시나리오">
  <span class="lbl">시나리오</span>
  <button data-mode="merge">Merge</button>
  …
</span>
```

알약 테두리로 감싸 "이건 한 묶음"이 눈에 보인다. 라벨 자체가 그룹 이름이라
`role="group"` + `aria-label`도 같이 줬다.

| | 390px 헤더 높이 | 시나리오 |
|---|---|---|
| 전 | 113px (세 줄) | 4개가 한계 |
| 후 | 81px (두 줄) | 4개는 한 줄, **7개까지 두 줄** |

여유가 세 개 더 생겼다. 8개째부터 세 줄이 되지만 묶음 안에서 접히므로 흐트러지지 않는다.

**함정 둘.**

- `setMode`의 `[...$("modes").children]`가 **직계 자식만** 훑는다. 버튼이 묶음
  안으로 들어가면서 `aria-pressed`가 전부 안 먹었다 → `querySelectorAll("[data-mode]")`로
  교체. 클릭 쪽은 원래 이벤트 위임이라 그대로 동작했다
- CSS 순서. `.modes .group button`(특이도 0,2,1)이 기존
  `.modes button[aria-pressed="true"]`(0,2,1)와 같아서 **뒤에 오는 쪽이 이긴다.**
  선택 상태가 투명 배경에 덮여 사라졌다 → `.modes .group button[aria-pressed="true"]`를
  묶음 규칙 **뒤에** 명시

라벨이 바뀌었으므로 시나리오를 이름으로 부르던 곳도 함께 고쳤다 — 안내 24·25단계
본문, README 세 곳. "`시나리오 · Merge`" → "`시나리오` 묶음의 `Merge`".

검증: 다섯 버튼 클릭 모두 정상(선택 상태 배타), 네 시나리오 진행 순서↔로그 1:1 유지,
안내 26단계 유지, 스포트라이트가 묶음을 감싸는지 좌표로 확인(`groupInsideHole:true`).

**순서도 학습 순서로 바꿨다** — `Merge` → `Push` → `Clone` → `Rebase`.
`Rebase`는 해시가 바뀌고 "push한 브랜치엔 쓰지 마라" 같은 위험 개념이라 **맨 끝**으로
미뤘다. `Push`(매주 겪는다)를 `Clone`(프로젝트당 한 번)보다 앞에 뒀다.

대가가 하나 있다. `Merge`와 `Rebase`는 **같은 상황을 두 방식으로** 처리하는 짝인데
양 끝으로 갈라졌다. 그래서 안내 24단계와 README가 "묶음은 **왼쪽부터 배우는 순서**",
"맨 앞 `Merge`와 **맨 끝** `Rebase`를 **이어서** 돌려 보라"고 명시하게 고쳤다.
README의 문단 순서(Clone ↔ Push)도 버튼 순서에 맞췄다. **순서를 또 바꾸면 이 문구들도
같이 고쳐야 한다.**

## 22차 — 시나리오 여섯 개로 (Commit · PR 추가)

21차에서 묶음 구조를 만들어 자리가 생겼다. 양 끝을 채웠다.

**`Commit` — 담기와 저장은 다른 일 (맨 앞).** 입문 첫 주에 모두가 만나는 거절이다.

```
git commit -m "적 AI 추가"   ✗ nothing added to commit
git add .
git commit -m "적 AI 추가"   ✓
git add . + git commit       한 번 더 — 짝이라는 걸 굳힌다
```

14차에서 스테이징 최소 모델을 넣으며 *"담지 않고 눌러 거절당해 보는 게 이 모델의
핵심"*이라고 적어 놓고도 정작 그걸 보여주는 시나리오가 없었다. 자유 모드에서
우연히 눌러야 만났다. `cmdCommit`을 `auto` 없이 부르면 그대로 거절하므로 새 명령은
필요 없었고, 안내 4단계처럼 `$("msg").value`를 채워 주기만 하면 됐다.

**`PR` — 합치기를 서버에서 (`Push` 뒤).** `Merge`(내 컴퓨터)와 대비된다. 안내 2부가
이미 PR을 실연하므로 겹침을 줄이려 `fetch`/`pull` 개념은 빼고 **"합치는 일이 어디서
일어나는가"**에만 집중시켰다. 브랜치 → push -u → PR 병합 → switch → pull, 6단계.

순서는 `Commit` → `Merge` → `Push` → `PR` → `Clone` → `Rebase`. 앞의 둘은 혼자 하는
일, `Push`부터가 팀 작업이다. `PR`은 원격을 배운 `Push` 직후가 맞다.

**기존 버그를 하나 잡았다.** `cmdCommit`의 거절 경로만 `render()`를 부르지 않아
빨간 로그가 화면에 **즉시 안 떴다**. `cmdPush`·`cmdSwitch`의 거절은 부르는데 여기만
빠져 있었다. 자유 모드에도 있던 문제인데, 거절 로그가 주인공인 `Commit` 시나리오를
만들자마자 드러났다 — 1단계를 눌러도 "아직 실행한 명령이 없습니다"가 그대로였다.

- 390px에서 묶음이 두 줄(118px)로 접힌다. 21차 측정대로 7개까지 여유가 있으니 하나 더 들어간다
- `setMode`의 원격 조건에 `pr` 추가 (`fresh(m === "free" || m === "push" || m === "pr")`)

검증: 여섯 시나리오 전부 진행 순서↔로그 1:1 일치, 의도된 거절은 `Commit`·`Push`
각 1건, 자유 모드에서도 거절 로그가 즉시 뜨는 것 확인, 안내 26단계 유지.

## 23차 — 서버에만 있는 역을 서버 구역으로

"팀원이 원격에 커밋했는데 **왜 그림에서는 로컬에 있나**" — 정당한 지적이었다.

이 앱은 역을 위쪽 본문에 한 번만 그리고 로컬/서버는 **선 종류로만** 구분해 왔다
(실선=내 것, 파선=서버에만). 규칙상으로는 맞지만 그 역이 `내 컴퓨터 (로컬)` 머리글
**아래** 놓여 있으니, 파선을 놓치면 "내 컴퓨터에 있는 흐릿한 것"으로 읽힌다. 구역
이름과 실제 위치가 어긋나 보였다. 꼬리표를 붙여 때우는 안도 있었지만 **정확한 쪽**을
골랐다 — 위치 자체를 옮긴다.

**줄 종류를 셋으로 늘렸다.**

| | 뜻 | 자리 |
|---|---|---|
| `L` | 내 컴퓨터에 있는 역 | 위 |
| `G` | rebase 후 버려진 역 | 가운데 (로컬에서 생긴 것이므로) |
| `R` | **서버에만 있고 아직 안 받은 역** | 맨 아래 = 서버 구역 |

`rowKey`가 `rowKind(id) + lane`이 되고 정렬이 `L → G → R`로 바뀌었다. `unfetched`를
줄 배치에서 쓰게 됐으므로 `gotten` 계산을 **파이프라인 3번으로 끌어올렸다**(전에는
이름표 다음이었다).

`bandTop`은 더 이상 "본문 아래 고정"이 아니다. 서버 전용 줄이 있으면 **그 줄 위로
올라와** 역까지 회색 구역에 담고, 없으면 예전 그대로다. 배경 rect 높이도
`BAND_H` 고정에서 `maxY - bandTop`으로 바꿨다.

**머리글이 역 메시지를 덮었다.** 경계를 올리자 "팀 공용 서버 (origin)" 머리글과 첫
서버 역의 커밋 메시지가 같은 높이에서 부딪혔다. `R` 줄만 `R_CAP`(26)만큼 더 내려
머리글 자리를 비웠다. `bandTop`도 `역 y - 92`(메시지·해시 53 + 머리글 26 + 여유)로
잡는다.

덤으로 **clone 전 화면이 정확해졌다.** 로컬이 통째로 비므로 `내 컴퓨터` 구역이 머리글만
남은 얇은 띠가 되고, 역 셋이 전부 서버 구역에 들어간다. "내 저장소가 아직 없다"가
그림 그대로다.

검증(전 상황 좌표로 확인): 머리글이 역 텍스트를 덮지 않음, 서버 역이 구역 안,
로컬 역이 경계 위 — clone 전/후, push 팀원커밋/완료, PR 병합/완료, 자유 모드
fetch 전후 모두 통과. 원격 없는 시나리오(commit·merge·rebase)는 `bandTop` 자체가
없어 기존과 동일하게 그려진다.

## 24차 — 받아온 역이 올라오는 이동 애니메이션

23차로 `pull` 전후에 역의 구역이 바뀌게 됐는데, 그 전환이 **한 프레임에 툭 끊겼다.**
받아왔다는 사실이 위치로 드러나는 게 23차의 핵심인데 정작 그 순간이 안 보였다.

`prevPos`(직전 render의 역 좌표)를 두고, 자리가 바뀐 역을 **이전 자리에 놓았다가
새 자리로 옮긴다**. 0.5초 `cubic-bezier(.32,.72,.28,1)`.

```js
g.style.transform = `translate(${pv.x}px,${pv.y}px)`;      // 이전 자리
requestAnimationFrame(()=>requestAnimationFrame(()=>{       // 두 번 기다려야 한다
  g.classList.add("node-move");
  g.style.transform = `translate(${p.x}px,${p.y}px)`;       // 새 자리
}));
```

**rAF를 두 번 도는 게 핵심이다.** 한 번이면 같은 프레임으로 묶여 전환 없이 점프한다.
이동은 바깥 `g`에만 건다 — 안쪽 `gi`는 등장 scale 담당이라 섞으면 4절 함정 그대로
요소가 날아간다.

**선로는 따라 움직일 수 없다.** 양 끝이 서로 다른 역에 붙어 있어 하나의 transform으로
끌 수가 없다. 자리가 바뀐 역에 닿은 선로는 새 자리에서 `edge-fade`로 떠오르게 했다.
등장 애니메이션(`edge-enter`)과는 둘 중 하나만 걸리도록 `else if`로 갈랐다.

`prevPos`는 `seenNodes`를 비우는 자리(`setMode` · `처음부터` · `tourRestore`)에서 함께
비운다. 안 비우면 모드를 바꿀 때 엉뚱한 역이 날아다닌다. 되감기에서 안 도는 것도
의도다 — 거꾸로 흐르면 어지럽다.

발동 지점을 좌표로 확인했다. `pull` 1개 · 자유 모드 `fetch` 1개 · `rebase` 2개에서
돌고, **커밋·팀원 커밋·push 거절·push 성공에서는 0개**다. 특히 거절에서 아무것도
움직이지 않는 게 맞다 — 거절은 상태를 바꾸지 않는다.

이동 궤적(실측): 218 → 195.0(250ms) → 192(완료), 그 뒤 자리 유지.
`prefers-reduced-motion`은 기존 전역 규칙이 `transition`까지 끄므로 그대로 대응된다.

## 25차 — 글자를 16px로, 버튼 라벨을 명령어로

강의실 뒷자리에서 안 보인다는 지적. 본문이 13px, 노선도 이름표가 11.5px로
웹 본문 기준(16px)보다 한 단계 작았다.

**본문 16px 기준(+23%)으로 올렸다.** 로그·범례 14.5, 노선도 이름표·역 메시지 14,
해시 13.5, 방금 친 명령 18.5, 안내 말풍선 17.

글자만 키우면 이름표가 알약을 넘친다. 알약(20→24)·흰 바탕(26→32)·HEAD 뱃지
(46×22→56×26)·`lblW` 여백(+20→+24)과 좌표 상수를 함께 올렸다 — `COL_W` 112→132,
`ROW_H0` 104→122, `LBL_GAP` 23→28, `BAND_CAP` 22→27, `R_CAP` 26→31.
**`LBL_FONT`(폭 실측용 문자열)도 14px로 같이** 맞춰야 한다.

### 조작판이 패널 밖으로 30px 삐져나갔다

`#freePanel`이 `grid-template-columns:1fr 1fr`인데 열이 `356px 248px`로 벌어졌다.
**`1fr`은 `minmax(auto,1fr)`이라** `white-space:nowrap` 버튼이 트랙을 밀어낸다.
`minmax(0,1fr)`로 못박아 균등 2열(285.8px)이 됐다.

### 머리글이 이름표를 덮었다

"팀 공용 서버 (origin)" 머리글이 `origin/main` 알약을 13px 덮었다. `render()`를
한 번 더 부르면 297px → 278px로 고쳐지는 게 단서였다.

`.map-scroll`의 `offsetTop`은 범례가 몇 줄로 접히느냐에 따라 달라지는데, **웹폰트가
적용되며 줄 수가 바뀌는 리플로우가 그 프레임에 아직 반영되지 않는다.** 첫 렌더의
19px 어긋난 값이 그대로 남아 있었다. 좌표 계산을 `placeZones()`로 떼어 내고 범례에
`ResizeObserver`를 걸었다.

### 버튼 라벨을 명령어로

`담기 · add` `커밋 · commit` 같은 병기와 `리베이스` 같은 음차가 섞여 있었다. 버튼은
`rebase`인데 아래 로그엔 `git rebase`가 찍히니 학생이 한 번 더 번역해야 했다.

**git 명령에 대응하는 9개만 명령어로 바꿨다** — `add` `commit` `switch -c` `switch`
`merge` `rebase` `fetch` `pull` `push`. 한글 뜻은 `title`로 옮겼다.

**명령이 아닌 버튼은 한글로 남겼다** — `팀원이 커밋`(시뮬레이션 장치) `PR 병합`
(GitHub 화면) `한 단계 되돌리기` `처음부터`(앱 조작). 전부 영어로 통일하자는 제안이
있었지만, 한글로 남는 것 자체가 **"이건 git 명령이 아니다"라는 신호**라 반대했다.
`팀원이 커밋`을 영어로 쓰면 학생이 그런 명령을 찾아 나선다.

라벨을 바꾸니 버튼을 이름으로 부르던 문구 7곳이 어긋나 함께 고쳤다
(거절 안내 6곳 + 안내 9단계 본문).

### 확인

6개 시나리오 전 구간에서 이름표 넘침 0 · 텍스트 겹침 0 · 머리글 여유 6px.
1440px(2열)과 1280px(1열) 모두 노선도 가로 스크롤 없음, 390px에서 시나리오 묶음
2줄 유지 · 버튼 글자 잘림 없음.

조작판을 620px로 넓혔더니 Rebase 그래프(781px)가 48px 넘쳐 **560px로 되돌렸다.**
라벨이 짧아진 덕에 560px로도 잘림이 없다.

## 26차 — PR을 열기와 병합으로 나눴다

`PR 병합` 버튼 하나만 있으니 초보자가 **PR을 병합할 때 쓰는 것으로 오해**한다는 지적.

확인해 보니 뿌리가 더 깊었다. `cmdPROpen()`(PR 열기, 저장소를 전혀 안 바꿈)은 이미
있는데 **시나리오와 자동 안내에서만** 불렸다. 자유 모드에는 병합 버튼만 있으니,
PR을 열지도 않았는데 병합부터 보이는 화면이었다.

`PR`로만 줄이는 안이 먼저 나왔지만 **반대했다.** 그 버튼은 PR을 만드는 게 아니라
이미 열린 PR을 승인해 합치는 동작이라, `PR`이라고만 쓰면 "누르면 PR이 생기겠구나"로
읽혀 오해가 뒤집힐 뿐이다.

`PR 확인` / `PR 처리` 안도 검토했다. "열기와 병합만이 PR의 전부는 아니다"라는 문제
의식은 맞지만 — `확인`은 누르면 PR이 **생기는** 동작과 어긋나고, `처리`는 승인·거절
까지 있는 것처럼 보이는데 이 앱은 병합만 한다(7절에서 의도적으로 자른 범위다).
**버튼 라벨은 "누르면 무슨 일이 일어나는가"를 맡고, PR의 넓은 그림은 설명문이
맡는다** — PR 시나리오 도입부가 이미 리뷰·승인 주체를 다루고 있다.

결론은 **`PR(열기)` / `PR(병합)`**. 괄호라 둘 다 PR의 일부로 묶여 보이면서 동작은
갈린다. 조작판에 버튼을 잇고(`btnPROpen`), 안내 18단계가 `.cmdbar` 대신 실제 버튼을
가리키게 했다.

고를 게 없을 때 빈 이름이 들어와 **"는 아직 서버에 없습니다"** 라고 주어가 비는
자리도 막았다.

확인: 자유 모드에서 `PR(열기)`는 커밋 수·`origin/main` 모두 그대로, `PR(병합)`에서야
`origin/main`이 움직인다. 안내 18·19단계 스포트라이트가 각 버튼을 정확히 감싼다
(측정은 `getBoundingClientRect`가 transition 중간값을 주므로 1초 뒤에 읽어야 한다).
1440px 조작판 넘침·글자 잘림 없음.

## 27차 — 커밋 두 단계를 눈에 보이게

Commit 시나리오에서 `git add .`를 눌러도 **화면에 아무 변화가 없었다.** 노선도는
그대로고 로그에 한 줄 찍힐 뿐. 담긴 것이 어디 갔는지 볼 자리가 없었다.

원인은 단순했다. `담긴 변경` 박스(`#stagedBox`)가 **`#freePanel` 안에 있는데
시나리오 모드에서는 그 패널이 통째로 `display:none`**이다. 상태(`S.staged`)는
정확히 관리되고 있었지만 보여줄 자리가 없었다.

### 대합실 → 승강장 → 노선도

교재의 `작업폴더 → staging area → repository` 그림을 **지하철 은유로 옮겨** 3칸 띠를
노선도 위에 얹었다. 승객(점 하나)이 지금 어느 구역에 서 있는지가 전부다.

| 칸 | 언제 | 뜻 |
|---|---|---|
| 대합실 | 입력칸에 글만 있음 | 적었지만 아직 안 담김 |
| 승강장 | `S.staged` 있음 | `add`로 담긴 상태 |
| 노선도 | 커밋 직후 950ms | 역이 생기는 순간 |

`add`·`commit` 화살표는 그 구간을 **방금 건넜을 때만** 진해진다.

커밋 직후에는 갈 곳이 "없음"뿐이라 `flowHold`로 노선도 칸을 잠깐 붙잡는데,
**다음 동작이 오면 즉시 놓아준다.** 처음엔 타이머만 두었더니 `add`를 눌러도 띠가
노선도에 머물렀다.

### 두 번 밟은 함정

`flowUpdate()`를 `render()` **뒤쪽**에서 부른 게 화근이었다. 띠가 뜨고 지는 것만으로
`.map-scroll`의 `offsetTop`이 101px 움직이는데 `placeZones()`가 먼저 계산돼,
Commit → 자유 모드로 바꾸면 구역 머리글이 **62px 겹쳤다.** 25차에서 고친 바로 그
버그가 새 조건으로 재발한 것이다. `flowUpdate()`를 `render()` 맨 앞으로 올리고
`ResizeObserver` 대상에 `#flowBar`를 추가했다.

측정에서도 같은 함정을 밟았다 — 점이 `opacity:0`으로 나와 CSS가 안 먹은 줄 알았는데
transition(0.35s) 진행 중 값이었다.

### 단계를 8개로

`add`와 `commit`을 묶은 단계가 둘 있었다(2·6단계). 전부 풀어 6 → 8단계가 됐다.
띠가 한 칸씩 움직이는 걸 매번 보게 하는 게 요점이다.

```
1 git init                      대합실
2 git add .                     → 승강장
3 git commit -m "최초 커밋"      → 노선도 (역 생성)
4 git commit -m "적 AI 추가"  ✗거절  대합실에 머묾
5 git add .                     → 승강장
6 git commit -m "적 AI 추가"    → 노선도
7 git add .                     → 승강장
8 git commit -m "점프 물리"      → 노선도
```

**4단계에서 띠가 대합실에 머무는 것**이 "적기만 하고 담지 않았다"를 말없이 보여 준다.

## 28차 — 충돌 시나리오

"`UI 폰트 교체`와 `점프 물리`가 충돌하지 않느냐"는 질문에서 시작했다. 답은 "같은
파일의 같은 곳을 고쳤다면 그렇다"인데, **이 앱에는 파일이라는 개념이 없어** 보여 줄
수가 없었다.

### 파일 *이름* 하나만 들였다

커밋에 `file` 한 칸을 더했다. 내용은 없다 — 충돌 판정에 필요한 건 **"갈라진 뒤 양쪽이
같은 파일을 고쳤나"** 하나뿐이고, 그게 학생에게 가르칠 핵심이기도 하다.

```js
{ id, parents, lane, msg, color, file:"style.css" }
```

`file`이 없는 커밋끼리는 절대 충돌하지 않으므로 **기존 시나리오 여섯 개는 손대지
않고 그대로 돈다.**

### 멈춘 병합

충돌이 나면 `cmdMerge`가 환승역을 만들지 않고 `S.conflict`에 멈춘 상태를 남긴다.
노선도에는 두 head를 잇는 **빨간 점선과 "충돌" 표시**만 그려진다 — 환승역은 아직
없다는 게 요점이다.

해결은 **고르기 → `add` → `commit`** 셋이다. 고르는 것은 "편집기에서 파일을 고치는"
일이라 저장소를 안 바꾸고, `add`가 "해결했다"는 표시이며, `commit`에서야 환승역이
생긴다. 하나라도 건너뛰면 거절하고 `#cfxStep`이 ①②③으로 지금 어디인지 알려 준다.

충돌 중에는 `switch`·`branch`·`merge`·`rebase`·`push`·`pull`을 막았다(`conflictGuard`).
실제 git도 `you need to resolve your current index first`라며 거절한다.

### 같은 함정을 또 밟았다

해결 버튼의 폭이 0으로 나왔다. `#cfxBox`를 `#freePanel` 안에 넣었는데 **시나리오
모드에서는 그 패널이 `display:none`**이다 — 27차에 `stagedBox`가 당한 바로 그 자리다.
밖으로 뺐다.

`CONFLICT` 줄에 `$`가 붙는 것도 고쳤다. 거절 로그(`"e"`)는 `$`를 붙이는데 이 줄은
명령이 아니라 **git이 뱉은 출력**이다. 붉은 출력 종류 `"x"`를 새로 뒀다.

"충돌" 알약이 아래 역의 메시지를 덮어 위치도 옮겼다(중점 → 위 역 쪽 0.3 지점).

### 확인

8단계 전 구간에서 5단계에 멈추고(커밋 수 3 유지) 8단계에 부모 둘인 환승역이 생긴다.
순서를 건너뛴 네 경우와 다른 명령 네 개가 모두 거절되고, 되돌리기는 고른 쪽까지
복원한다. 시나리오 일곱 개 전부 오류 없이 완주하며, 파일을 안 적은 자유 모드 merge는
여전히 충돌 없이 합쳐진다.

## 29차 — 학습 시나리오에 번호

시나리오가 일곱 개가 되자 "왼쪽부터 배우는 순서"라는 말만으로는 순서가 안 읽혔다.
묶음 이름을 **학습 시나리오**로 바꾸고 버튼마다 번호를 붙였다.

```
학습 시나리오 │ 1 Commit  2 Merge  3 Conflict  4 Push  5 PR  6 Clone  7 Rebase
```

드롭다운으로 접는 안도 나왔지만 **버튼 나열을 유지했다.** 강의에서 "3번 눌러
보세요"라고 할 때 바로 가리킬 수 있어야 하고, 무엇보다 **자동 안내 25단계가 이
버튼들을 스포트라이트로 밝힌다** — 닫힌 메뉴는 밝힐 수가 없다. 열 개를 넘기면 그때
다시 볼 일이다.

### 390px에서 세 줄로 접혔다

번호 알약이 버튼당 22px씩 더해 두 줄을 넘겼다. 라벨을 자기 줄로 올려도 소용없었고
(오히려 155px로 늘었다), **알약 배경을 빼서** 두 줄로 돌아왔다. 좁은 화면에서는
번호가 작은 회색 숫자로만 남는다.

안내 25·26단계와 README도 번호로 부르게 고쳤다 — `2번 Merge`·`7번 Rebase`·
`6번 Clone`.

## 30차 — 자동 안내에도 충돌

시나리오로만 두면 안내를 끝까지 본 학생이 충돌을 **한 번도 안 겪는다.** 2부 마지막
(환승역이 생긴 직후)에 세 단계를 넣었다. 27 → 30단계.

| 단계 | 하는 일 |
|---|---|
| 23 이번엔 같은 파일을 고쳐 봅니다 | `feature-ui`를 파고 양쪽에서 `style.css` 커밋 |
| 24 합치려는데 멈춥니다 | `merge` → CONFLICT, 빨간 점선 |
| 25 고르고, 담고, 커밋 | `둘 다` → `add` → `commit` → 환승역 |

여기까지 자동으로 합쳐진 게 **양쪽이 서로 다른 파일을 고쳤기 때문**이라는 걸 23단계가
짚어 준다 — 그 전까지는 왜 안 막혔는지 설명한 적이 없었다.

**25단계는 `.map`을 가리킨다.** 처음엔 `#cfxBox`(충돌 해결 상자)를 가리켰는데, 그
단계의 `act`가 충돌을 끝내 버려 **스포트라이트가 잡을 대상이 사라졌다.** 결과가 남는
노선도로 바꿨다.

검증 중에 자동 재생이 도는 바람에 단계가 넘어가 엉뚱한 좌표를 쟀다. `tPlay`로 멈춘 뒤
재야 한다. 좁은 화면(495px)에서 스포트라이트가 어긋나는 것은 **기존 단계도 마찬가지**라
이번 변경과 무관하다 — 1440px에서는 새 단계 셋 다 대상을 정확히 감싼다.

## 31차 — 해시가 무엇인지 설명

커밋 메시지 아래 일곱 자리를 **이름만 부르고 뜻을 설명한 적이 없었다.** 안내 2단계는
"커밋 해시가 적혀 있습니다"라고만 했고, `rebase` 설명은 "해시가 바뀐 점을 보세요"라고
했다.

`7 Rebase`의 핵심이 **"복사되면서 해시가 바뀐다"** 인데, 해시가 뭔지 모르면 그 위험이
와닿지 않는다. "숫자가 좀 바뀌었네"로 넘어간다.

두 곳에 나눠 심었다.

- **안내 2단계** — 해시가 처음 보이는 자리. "git이 커밋 내용에서 계산해 붙이는 고유한
  이름표라, 사람이 정하지 않고 **내용이 바뀌면 해시도 달라진다**"
- **`cmdRebase` 설명** — 그 사실을 받아 결론으로 잇는다. "메시지가 같아도 해시가 다르면
  git에게는 **다른 커밋**이다. 남이 이미 받아 간 커밋을 이렇게 바꾸면 서로 다른 역사를
  갖게 되고, 그래서 **push한 브랜치에는 rebase를 쓰지 않는다**"

3부 "기호 읽는 법"에 넣는 안도 있었지만, 거기는 **rebase를 언급하는 28단계보다 뒤라서
늦다.** 먼저 심고 나중에 거두는 순서가 맞다.

2단계 본문이 길어져 말풍선이 490px가 됐지만 390px 화면에도 들어간다.

## 32차 — 충돌 선택 덮어쓰기 수정과 전체 점검

"상대 것을 골랐는데 둘 다로 나온다"는 제보. **시나리오 6단계가
`cmdResolve("both")`를 무조건 실행해 학생의 선택을 덮어쓰고 있었다.**

바로 앞서 "선택 줄이 두 번 찍힌다"는 제보를 받고 로그를 갈아 끼우도록 고쳤는데,
그 탓에 흔적까지 사라져 증상이 또렷해졌다 — **중복 로그는 증상이었고 뿌리는 이쪽**이었다.

```js
if(S.conflict && !S.conflict.side) cmdResolve("both");   // 안 골랐을 때만
```

진행 순서 문구도 `양쪽 내용을 모두 남겼습니다` → `남길 쪽을 정했습니다`로 중립화했다.
무엇을 고르든 맞는 문장이어야 한다.

### 같은 실수가 또 있는지 전체 점검

| 항목 | 범위 | 결과 |
|---|---|---|
| 거절 경로 | 17가지 | 전부 상태 안 바뀜 |
| 되돌리기 | 시나리오 7개 완주→역주행 | 앞뒤 대칭, 잔여 없음 |
| 안내 중 학생 개입 | 7가지 선행 조작 | 오류 0 |
| UI ↔ 상태 일치 | 셀렉트 4개·HEAD·담김·모드 버튼 | 전부 일치 |
| 자유 모드 버튼 | 16회 연속 | 오류 0 |
| 모드 전환 초기화 · `#step=` 재개 | 8개 모드 | 정상 |

**같은 유형은 더 없었다.** 시나리오 중에는 `#freePanel`이 통째로 숨어 학생이 누를 수
있는 게 진행·모드 버튼뿐이고, **밖에 둔 `#cfxBox`만 유일한 통로**였기 때문이다.
시나리오에서도 보이게 하려고 밖으로 뺀 구조가 그대로 대가가 됐다.

`prevPos`가 모드 전환 뒤에도 차 있어 잔재인가 했지만 **오탐**이었다 — `setMode`가 비운
뒤 `render()`가 현재 좌표로 다시 채운 것이다.

남은 취약점 하나를 문서에 적었다. **`stagedBox`는 아직 `#freePanel` 안에 있어**,
시나리오에서 `add`만 하는 단계를 새로 만들면 담긴 상태가 안 보인다. 지금은 흐름 띠와
`#cfxStep`이 우연히 가려 주고 있을 뿐이다.

## 33차 — Codex 리뷰가 찾은 것 수정

외부 리뷰(Codex)를 돌려 32차 점검이 놓친 것을 받았다. 지적 9건 중 **정상 UI로 도달
가능한 것은 둘**이었고, 나머지는 도달 불가이거나 정책 문제였다.

### 모드 버튼의 번호를 누르면 안 먹었다

```js
$("modes").onclick = e=>{ if(e.target.dataset.mode) …   // ✗ <i>에는 data-mode가 없다
$("modes").onclick = e=>{ const b = e.target.closest("[data-mode]"); …   // ✓
```

번호 알약 `<i>`가 클릭 대상이 되면 `e.target`에 `data-mode`가 없어 **조용히 무시**됐다.
시나리오 버튼 7개 전부 해당(`자유 모드`만 `<i>`가 없어 무사했다). 번호가 6~8px로 좁아
지금까지 안 드러났을 뿐이다.

**32차 점검이 이걸 놓친 이유** — `setMode()`를 직접 호출해 검증했으니 이벤트 위임
경로를 아예 안 탔다. 코드를 읽은 리뷰만 잡을 수 있는 자리였다.

### `snap()`의 기준을 "로그를 바꿨나"로 통일

거절은 `snap()` 없이 로그만 바꾸고, 조회(`git branch -a`·PR 열기)는 저장소를 안 바꾸는데
`snap()`을 불렀다 — **기준이 정반대로 섞여 있었다.**

거절 쪽 증상이 더 나빴다. 되돌리기 한 번에 **거절 줄과 그 앞의 실제 커밋이 함께**
사라졌다(커밋 2 → 1). 학생 눈에는 "한 번 눌렀는데 두 칸 갔다"로 보인다.

로그가 `S` 안에 있는 이상 **로그 한 묶음이 되돌리기 한 칸**이다. 이 기준으로 세 거절
경로에 `snap()`을 넣었고, 조회 쪽은 원래대로 두어 양쪽이 같은 규칙이 됐다. `cmdPush`의
"올릴 것 없음"은 `render()`도 빠져 있어 함께 고쳤다.

### 조기 반환에서 빠진 `render()`

`say()`는 DOM을 직접 건드려 설명만 즉시 바뀌고, 로그와 명령 띠는 **다음 렌더까지 이전
명령에 멈춰 있었다.** 수정 전 `merge` 이미 최신을 실행하면 명령 띠가 `git switch main`을
그대로 달고 있었다 — 학생 눈에는 "눌렀는데 아무 반응이 없다"로 보인다.

세 곳(`merge` 이미 최신 · `fetch` 새 것 없음 · `pull` 이미 최신)에 `render()`를 넣었다.
`pull` 쪽은 `r.known = r.head`로 **저장소를 실제로 바꾸면서도** `snap()`이 없어 되돌릴
수도 없었다 — 같이 넣었다.

수정 뒤 **로그를 바꾸는 14개 경로를 전수 확인**했고 누락은 없다. 처음에는 `S.log` 항목
수와 로그 DOM의 `div` 개수를 비교했는데, 머리말 묶음이 한 `div`에 여러 줄로 들어가 전부
"어긋남"으로 잘못 나왔다 — **마지막 로그 줄이 화면 텍스트에 있는지**로 바꿔야 맞다.

### 고치지 않기로 한 것

- **`cmdUndo`가 추적 집합을 정리하지 않는다**(6번) — 사실이다. `cmdUndo`를 6회 반복하면
  `seenNodes` 7개 중 6개가 죽은 ID다(`btnPrev`는 0개). 하지만 `prevPos`는 `render()`
  끝에서 현재 좌표로 다시 채워지고, `hash()`가 매번 새 값을 뽑아 죽은 ID가 재사용되지도
  않는다. rebase 완주 후 되돌린 화면을 두 경로로 비교했더니 **최종 역 수가 같았다.**
  되돌리기 세 경로를 손볼 때 함께 정리하는 게 낫다
- **안내 복원이 `prevPos`를 비운다**(7번) — `btnPrev`도 같은 정책이다. 비우는 것은
  "없어진 역에서 날아오는 유령 이동 방지"라는 **의도된 처리**다. 어긋난 쪽은 오히려
  `cmdUndo`고 그건 6번이다
- **`note`가 `S` 밖에 있다**(9번) — 되돌린 뒤 문구가 `한 단계 되돌렸습니다`로 덮이는데
  **무엇을 되돌렸든 참인 문장**이라 모순이 생기지 않는다. 충돌 상태에서 되돌려도
  설명·`cfxBox`·`S.conflict` 셋이 함께 움직인다. 시나리오가 `note`를 복원하는 것은 정해진
  단계를 오가기 때문이고, 자유 모드는 되돌아간 지점에 붙은 설명이 없으니 동작만 알리는
  게 맞다

### 도달 불가로 판정한 것

- **시나리오 '이전 단계'와 일반 Undo의 시간선 불일치** — 코드상 사실이지만 `btnUndo`가
  `#freePanel` 안이라 시나리오 모드에서 **0×0으로 숨는다.** 모드를 나가면 양쪽 스택이
  다 비워져 섞일 경로가 없다
- **자동 안내의 550ms 지연 창** — 그 사이 학생이 조작하면 덮어쓰기·중복 실행이 난다는
  지적. 실제로는 `.tour-mask`가 `pointer-events:auto`로 **조작판 전체를 덮어** 마우스로는
  아무것도 못 누른다(`elementFromPoint`가 전부 `tour-mask`를 돌려준다). 처음에 "재현됨"
  으로 봤던 것은 JS로 값을 직접 넣은 결과였다 — 실사용 경로가 아니다. 키보드 포커스는
  이론적으로 가능하나 안내 중 Tab으로 입력칸에 가는 경우는 사실상 없다

### 수정 뒤 전체 재점검

| 항목 | 범위 | 결과 |
|---|---|---|
| 거절 경로 | 18가지 | 상태 안 바뀜 + 로그가 화면에 반영됨 |
| 시나리오 되감기 | 7개 완주→역주행 | 전부 대칭, 충돌 잔여 없음 |
| 되돌리기 한 칸 | 거절 3 + 조회·조기반환 4 | 로그 한 묶음만 되돌아감 |
| 자동 안내 | 30단계 수동 완주 | 커밋 1→8, 환승역 2개, 오류 0 |
| UI ↔ 상태 | 셀렉트 3개·HEAD·모드 버튼 | 전부 일치 |
| 모드 전환 | 8개 모드 | 스택·충돌·담김 잔여 없음 |
| 렌더링 | 1440px · 390px × 8개 모드 | 가로 스크롤 없음 |

안내를 90ms 간격으로 넘겼더니 커밋이 하나도 안 늘어 처음엔 고장인 줄 알았다. **550ms
지연 동작이 다음 `tClear`에 취소된 것**이다(4절에 적힌 함정 그대로) — 단계 간격을
760ms로 늘려야 실제 동작을 검증할 수 있다.

`cur()` 널 참조와 XSS는 리뷰에서도 실제 경로를 찾지 못했다. 다만 `cmdBranch()`가 저장소
검사 없이 `cur().head`를 읽는 것은 함수 수준의 취약점으로 남아 있다(조작판이 숨어 정상
UI로는 도달하지 않는다).

## 34차 — 디자인 리뷰가 찾은 것 수정 (v1.0 · v1.0.1)

리뷰를 두 번 돌려 나온 것을 고쳤다. 인상을 바꾸는 작업은 하지 않았다 — 노선도 은유와
팔레트는 손대지 않는 것이 규칙이고, 리뷰도 "이 디자인은 이 제품 전용으로 만들어졌다"고
판정했다. 실제로 한 일은 **자기 규칙을 스스로 깨고 있던 자리를 메우는 것**이었다.

### 화면이 문서를 배신하고 있던 자리 셋

**명령 띠가 CONFLICT를 명령처럼 보여 줬다.** 로그에서는 `x`(git이 뱉은 붉은 줄)에 `$`를
붙이지 않는 규칙을 지켰는데, 노선도 위 명령 띠는 같은 줄을 `$ CONFLICT (content): …`로
그렸다. 화면에서 가장 큰 글씨가, 이 도구의 성공 기준("로그를 그대로 터미널에 옮겨 치기")을
정면으로 깨고 있었다. 명령을 고를 때 `"o"`와 함께 `"x"`도 건너뛰게 했다.

**390px에서 페이지가 옆으로 16px 넘쳤다.** `#freePanel`의 `1fr`이 실제로는
`minmax(auto,1fr)`이라 `nowrap` 버튼 행이 트랙을 밀어낸 것 — 1400px 미디어 쿼리에서는
이미 `minmax(0,1fr)`로 막아 두었는데 **기본 규칙에서만 빠져 있었다.** 함정을 알고 적어
두었는데도 한 곳을 놓친 셈이다.

**빈 상태 문구가 대비 2.9였다.** `#9AA6B4` — 퇴출하기로 적어 둔 `#9AA5B1`과 사실상 같은
색이다. Commit 시나리오의 출발 화면을 설명하는 유일한 텍스트인데 뒷자리에서 안 읽혔다.

> **"N군데를 함께 고쳐야 한다"를 문서에 적는 것으로는 부족하다.** 셋 다 규칙이 이미
> 문서에 있었고, 그래도 어긋났다. 이 셋 중 앞의 둘은 기계가 볼 수 있는 종류다.

### CSS 특정도 싸움에 져서 죽어 있던 CTA

두 번째 리뷰가 첫 번째가 놓친 것을 잡았다. `#btnTour`가 `.modes` 안에 있어
`.modes button`(특정도 0,1,1)이 `.tour-cta`(0,1,0)를 덮고 있었다. 계산된 값을 재 보니
배경이 흰색, 글자가 회색, 굵기 600 — **미선택 모드 알약과 완전히 같았다.**

`DESIGN.md`에는 분홍 배경·흰 글씨·800으로 적혀 있다. 문서와 화면이 달랐고, 첫 사용자에게
안내를 권하는 입구가 화면에서 가장 흐린 버튼이었다. 선택자를 `.modes .tour-cta`로 함께
적어 0,2,0으로 올렸다.

고친 뒤 곧바로 회귀가 하나 나왔다. 전역 포커스 링이 분홍인데 배경도 분홍이 되어 링이
사라진 것이다. 이 버튼만 잉크색 링으로 바꿨다.

> **스크린샷으로는 안 보이는 종류의 고장이 있다.** 흰 버튼도 그 자리에서는 그럴싸해
> 보인다. 계산된 스타일을 읽지 않았으면 두 번째 리뷰도 못 잡았을 것이다.

### 되돌릴 수 없던 두 자리

`setMode()`가 자유 모드에서 쌓은 그래프와 `undoStack`을 통째로 비웠다. 시나리오 알약을
잘못 누르면 복구 수단이 0이다. `처음부터`도 마찬가지로 `undoStack`을 지웠다 —
`한 단계 되돌리기` **바로 옆에 같은 크기로** 있는 버튼이 그랬다.

README는 "잘못 눌러도 마음껏 망가뜨려도 됩니다"라고 약속하고 있었다.

확인 대화로 막는 대신 되돌릴 수 있게 했다. 자유 모드를 떠날 때 상태를 한 칸 보관하고,
`처음부터`는 리셋 직전 상태를 `undoStack`에 남긴다. **눌러 보게 두는 것이 이 도구의
방식이므로, 막는 쪽이 아니라 돌아올 수 있는 쪽을 골랐다.**

보관을 넣자마자 안내와 부딪혔다. `tourStart()`가 보관본을 복원한 위에 안내를 재생하면
같은 커밋이 두 번 붙는다 — `tourJump`가 이미 겪은 함정이라 같은 자리에서 막았다.

### 그 밖에

- 안내 말풍선을 대화상자로. 포커스가 마스크 뒤 페이지에 남아 Tab이 어두운 컨트롤을 돌고
  있었다. 진행 막대 30칸이 전부 Tab 스톱이라 '다음'으로 돌아오는 데 **34번**을 눌러야
  했다 — 지금 칸만 Tab으로 들어오고 칸 사이는 화살표로 옮긴다(4번)
- `Esc`가 `#step`까지 지워 재개 지점이 사라졌다. 실수로 눌리기 쉬운 키라 해시를 남긴다
- 명령 띠를 900px 이하에서 숨기지 않고 32px로 줄인다. "사이드바가 가까워서 숨겼다"는
  주석의 전제가 실제 DOM 순서와 달랐다 — 한 열로 접히면 로그는 세 화면 아래다.
  거절의 빨간 띠가 폰에서 유일한 즉시 신호인데 뷰포트 폭 하나로 사라지고 있었다
- 일부러 거절당하는 단계에 붉은 `✗`. 로그와 명령 띠는 거절을 구분하는데 진행 순서만
  성공과 같은 `✓`였다
- 시나리오 끝이 비활성 버튼으로 끝났다. 번호가 학습 순서라는 자산을 정작 끝에서 안 썼다 —
  `SCEN_ORDER`를 두고 "다음은 4번 Push"를 가리킨다
- 좁은 화면에서 `다음 단계`와 노선도가 세 화면 떨어져 있어, 누른 뒤 바뀐 그림으로 데려간다
- 폰트 스택 첫 항목 `"Pretendard Variable"`이 **지금까지 로드조차 안 되고 있었다.**
  static CSS는 `Pretendard`만 정의한다. 가변 서브셋으로 바꿨다. 한국어 줄바꿈은 어절 단위로
- `lang="ko"`, `<main>`, 폼 라벨 여섯, `#note`에 `aria-live`, 빈 `<select>` 안내문,
  진행 막대 터치 높이 24px, 커밋 메시지·브랜치 이름 길이 상한

### 남긴 판단

노선도 안 글자(역 메시지 14px·해시 13.5px)가 UI 본문 16px보다 작다. 시연이 주 장면인데
정작 봐야 하는 것이 가장 작은 셈이다. 확대 토글을 미리보기로 만들어 재 봤더니
**Rebase 그래프는 어느 창에서도 여유가 없었다** — 1440px에서 1.05배부터, 1600px 이상에서도
1.2배부터 가로 스크롤이 생긴다. 의미 있는 확대는 전체 조망을 반드시 포기해야 한다.
강의실 프로젝터에서 읽히는지 확인한 뒤 판단하기로 두었다.

자유 모드 컨트롤 21개가 같은 무게로 노출되는 것도 남겼다. 1주차 학생에게 `rebase`와
`add`가 동급으로 보이지만, 전부 보여주는 정직함과 순서대로 여는 안내 중 무엇이 이
커리큘럼에 맞는지는 수업 운영 방침이 걸린 문제다.

## 35차 — 범례·구역 기둥·안내 말풍선 (v1.1)

화면에서 셋이 각각 자기 역할을 못 하고 있었다. 범례는 노선도와 한 덩어리로 보였고,
구역 이름은 시작점에 흐린 글씨로 얹혀 "어디까지가 내 컴퓨터인지"를 말하지 못했고,
안내 말풍선은 흰 상자라 화면에 떠 있는 또 하나의 패널로 읽혔다.

- **범례** — `--paper` 판 + 테두리로 떼어 놓고 앞에 `범례` 라벨. 역 안내판 형태
- **구역 머리글 → 왼쪽 세로 기둥** (44px, 모바일 37px). `text-orientation:upright`로
  글자를 세우고 잉크·`#46525F` 판에 흰 글씨. 길이가 곧 구역의 범위다
- **안내 말풍선** — 머리부(`.cap`)만 잉크 판 + 노선색 6등분 띠, 본문은 흰 바탕 유지.
  잉크 위에서 `--head`가 대비 3.82로 미달이라 `#FF6BA3`(6.44)·`#A9B4C0`(8.15)로 교체

**한 번 되돌렸다.** 첫 시도는 기둥 폭을 18px까지 짜내 그래프를 지키려 했고, 글자가
뭉개지고 두 기둥이 겹쳐 못 쓰게 됐다. 폭이 부족하다는 신호를 무시하고 밀어붙인 것이
원인이라, 두 번째는 **폭을 먼저 확보하고 그래프를 양보시켰다**. 실제로는 손해가 거의
없었다 — 기둥이 뜨는 모드(자유·Push·PR·Clone)는 그래프가 좁고, 넓은 Merge·Rebase·
Conflict는 원격이 없어 기둥이 안 뜬다. 스크롤이 없던 장면이 새로 스크롤이 된 곳은 없다.

작업 중 잡은 것 셋:

- **서버 기둥이 로컬 기둥을 덮었다.** clone 시작 화면에서 `내컴퓨터`가 `내컴`으로 잘렸다.
  `bandTop`이 24px — 로컬 커밋이 0개면 구역이 사실상 없었다. 하한을 84로 올리니
  "내 컴퓨터 칸이 비어 있다"는 설명과 화면이 비로소 맞았다
- **가로로 스크롤하면 역이 기둥 밑으로 들어갔다.** `padding-left`는 스크롤되는 내용의
  일부다. `margin-left`로 바꿔 스크롤 영역 자체를 밀었다. 정지 화면에서는 안 드러난다
- **세로쓰기는 넘쳐도 `scrollHeight`가 안 커진다.** 글자가 소리 없이 사라진다.
  높이를 재서 이름을 줄이는 `fitZone()`을 뒀다

검증은 1440px·390px 두 폭에서 8개 모드 × 전 단계를 순회해 기둥 겹침·글자 넘침·
가로 스크롤·기둥 침범을 자동 검사했다. 남은 것은 Rebase 8단계 38px 넘침 하나인데
**원본(`ebf67a5`)에서도 동일**해 이번 범위 밖으로 두었다.

> 이번 세션에서는 내장 브라우저와 Chrome MCP가 모두 연결되지 않아 스크린샷 없이
> 수치만으로 보고했다가 한 번 실패했다. Playwright MCP를 설치하고 나서야 실제 화면을
> 보며 고칠 수 있었다. **`CLAUDE.md` 1절 6항(스크린샷 없이 보고하지 않는다)이 그대로 옳다.**

### 병합선 두 개가 붙어 보이던 것 (같은 날, 미배포)

PR 병합 화면에서 병합 커밋으로 들어오는 두 선이 한 줄로 보였다. 둘 다 서버에만 있는
구간이라 같은 회색 파선이어서 색으로도 구분되지 않았다.

원인은 **끝점과 진입 접선이 모두 같은 것**이었다. 두 선의 도착 제어점이
`271.3 378`로 완전히 동일해 마지막 구간이 포개졌다. 처음에는 곡률만 바꿔 봤는데
(두 번째 부모 쪽 제어점을 역 가까이) **간격이 2~3px밖에 안 늘었다** — 같은 점에
같은 방향으로 들어오는 이상 곡률로는 벌릴 수 없다. 출발 쪽을 일찍 꺾는 시도는
선이 중간 역 위를 지나가 더 나빠져 되돌렸다.

두 번째 부모 선의 **도착 y를 9px 내려**(`EDGE_SPLIT`) 평행 간격을 남겼지만, 사용자
확인 결과 **여전히 "변한 게 없다"**였다. 9px로는 두 선이 나란히 붙은 것으로 읽혔다.

사용자가 직접 그려 준 안(한쪽을 크게 우회시키기)을 따랐다. 줄을 건너뛰어 오는
**첫 부모**(원래 있던 노선)를 출발 직후 거의 수직으로 내려보내 아래에서 완만히 돌아
들어오게 했다(`DETOUR_OUT`/`DETOUR_IN`). 두 선이 아예 다른 궤적을 그려 한눈에
갈라진다 — 지하철에서 두 노선이 한 역에 들어오는 방식과 같다.

같은 행에서 오는 부모는 우회하지 않는다. Merge·Conflict의 `main`은 수평 직선이
맞고, 그대로 유지되는 것을 확인했다. Rebase는 fast-forward라 병합 커밋이 없다.

> **간격을 수치로 벌리는 것과 "갈라져 보이는 것"은 다른 문제였다.** 9px은 측정상
> 분명한 개선이었지만 눈에는 여전히 한 다발이었다. 수치가 나아졌다고 해결로 보고하지
> 말 것 — 이번에도 사용자가 다시 보고 나서야 알았다.

> 측정 중 뷰포트가 1105px로 바뀐 걸 모르고 "여러 모드에서 넘침"이라 읽을 뻔했다.
> 1400px 미만은 원래 노선도 스크롤이 허용되는 구간이다. **폭 관련 수치를 읽을 때는
> `window.innerWidth`를 함께 찍을 것.**

### Rebase 8단계 가로 넘침 (같은 날, 미배포)

`git rebase main` 직후 노선도가 836px로 뻗어 1440px 화면에서 가로 스크롤이 생겼다.
원본부터 있던 문제인데(기둥이 52px를 가져가며 38px → 90px로 커졌다), 고칠 자리를
그래프가 아니라 **조작판**에서 찾았다.

시나리오 모드에서는 `#freePanel`(명령·원격 버튼 두 묶음)이 통째로 숨는데 조작판은
560px를 그대로 차지하고 있었다 — 빈 폭 옆에서 그래프가 잘리고 있었던 셈이다.
`setMode()`가 `body.scen`을 토글하고, 그 상태에서만 조작판을 420px로 좁힌다.
자유 모드는 560px 그대로여서 두 묶음이 나란히 서는 배치가 유지되고, 안내는 자유
모드 기반이라 영향이 없다.

420px은 미디어 쿼리가 막 켜지는 **1400px에서** 836px 그래프가 들어가는 값이다
(440px으로 잡았다가 그 폭에서 10px이 모자랐다). 1280·1400·1440·1600·390px에서
8개 모드 전 단계를 돌려 노선도 넘침·문서 가로 스크롤·조작판 요소 이탈이 모두 없다.

### v1.1.1 — 기둥이 안 보이던 것 (같은 날 후속)

사용자 보고: "시나리오 4·6 빼고 나머지는 구역 머리글이 잘려서 안 보인다."
원인이 둘이었다.

- **`hidden`이 안 먹었다.** `hidden` 속성은 UA 스타일시트의 `display:none`이라
  `.zone`에 준 `display:flex`에 진다. 숨겨야 할 Remote 기둥이 직전 크기 그대로
  남아 **잘린 채** 보였다. `.zone[hidden]{display:none}` 한 줄로 해결.
  v1.1이 내보낸 버그이고, 자동 검사가 `el.hidden` 속성만 보고 실제 `display`를
  안 봐서 놓쳤다 — 이후 검사는 `getComputedStyle().display`를 함께 본다
- **원격이 없는 넷(Commit·Merge·Conflict·Rebase)은 기둥이 아예 없었다.**
  `hasBand` 하나로 두 기둥을 함께 숨긴 탓. `Local`은 커밋이 있으면 언제나 서고
  `Remote`만 `hasBand`를 따르도록 분리했다

함께 **이름을 `Local` / `Remote` 영문으로** 바꿨다(사용자 요청). git 문서가 쓰는
말이라 `git branch -r`·`origin/main`에서 다시 만난다. 라틴 문자는 `upright`를 빼고
눕혀야 한다 — 세우면 낱자가 다섯 칸으로 쌓인다. 길이가 77px → 41px로 줄어
잘림 위험도 함께 사라졌다. `fitZone()`은 글자 수 계산을 버리고 `Range` 실측 +
폰트 축소로 바꿨다(누운 라틴 문자는 글자 수 × 폰트로 계산되지 않는다).

## 36차 — 검사·스냅샷 도구와 render() 분해 (v1.2)

화면은 한 픽셀도 안 바뀐 판이다. 앞으로 고칠 때 **조용히 깨지는 것**을 막는 데 썼다.

`render()`가 438줄이었다. 위험한 건 길이가 아니라 그 안의 두 자리였다.

```js
gapAt[d] = Math.max(...)   // 이름표 기준으로 한 번
gapAt[d] = Math.max(...)   // 서버 띠 기준으로 또 한 번 — 2패스
colX[d] += shift;          // 다 만든 뒤 바깥에서 제자리 이동
```

둘 사이에 코드를 끼우면 **밀리기 전 좌표**를 받는다. 예외도 로그도 없고 그래프만 조금
왼쪽으로 치우친다. 스크린샷을 봐도 "원래 저랬나?" 싶은 종류다. 이번 분해의 목적은
줄 수 줄이기가 아니라 이 두 자리를 **구조적으로 불가능하게** 만드는 것이었다.

### 도구를 먼저 만들었다

**`tools/check.mjs` — 교차 참조 검사 22개.** 이 문서와 `CLAUDE.md`가 "**손으로** 고쳐야
한다"고 적어 둔 자리들을 기계가 본다. 시나리오 번호는 `SCEN`·`SCEN_ORDER`·헤더 `<i>`·
안내 본문·README 다섯 곳에 흩어져 있고, 버튼 라벨은 `title`과 거절 문구가 이름으로 부른다.

돌연변이 8종(번호 뒤집기·라벨 변경·상수 변경·`SCEN_ORDER` 항목 삭제·`localStorage` 사용·
README 번호 오기·PR 버튼 한쪽만 개명·범례 기호 변경)을 넣어 전부 걸리는 것을 확인했다.
`SCEN_ORDER`에서 한 줄 지운 것이 **검사 5개를 연쇄로 터뜨리며** README 5줄까지 짚었다.

첫 실행에서 바로 하나 잡았다 — `CLAUDE.md` 머리말이 `index.html`을 1,420줄이라 적고
있었는데 실제로는 2,436줄이었다. **자기 자신에 대해 이미 틀린 문서였다.**

**`tools/snapshot.mjs` — 78개 상태의 그림 비교.** 시나리오 일곱 편 전 단계(52개)와 자유
모드 조작열을 돌며 SVG와 사이드 UI를 받아 적어 전후로 견준다.

하네스부터 검증했다. 같은 코드로 두 번 떴더니 **78개 중 75개가 어긋났다.** 커밋 해시가
난수였다. 해시를 가리는 것이 쉬웠지만 그러면 "해시가 바뀐다"를 가르치는 Rebase 8단계
설명문을 못 보게 된다 — 지우는 대신 `Math.random`에 씨앗을 심었다. 웹폰트도 막았다
(뜬 판과 안 뜬 판은 `textW()`가 달라 좌표가 통째로 밀린다). 그 뒤 78/78이 같았고,
`EDGE_SPLIT`을 1px 옮기면 18개가 어긋나는 것까지 확인했다.

> **통과만 하는 검사기는 아무것도 증명하지 않는다.** 두 도구 모두 "일부러 망가뜨려
> 잡히는지"를 본 뒤에야 신뢰했다. 이 확인을 건너뛰었으면 diff 0을 안심의 근거로 삼았을
> 것이고, 그건 검사를 안 한 것과 같다.

### 네 단계로 나눴다

매 단계 스냅샷 diff 0을 확인하고 커밋했다. 옮긴 줄은 한 글자도 고치지 않았다 —
인자를 구조 분해로 받아 본문이 쓰던 이름을 그대로 쓰게 했다.

| 단계 | `render()` |
|---|---|
| `syncPanel()` 분리 (사이드 UI 71줄, 레이아웃 값을 안 쓴다) | 438 → 368 |
| `paint(L)` 분리 (그리기 227줄) | 368 → 143 |
| 계산을 `graphOf`·`rowsOf`·`columnsOf`·`boundsOf` 넷으로 | 143 → **13** |

뒤 국면이 앞 국면의 결과를 **인자로 받으므로** 순서를 바꾸면 값이 조용히 틀리는 대신
그 자리에서 터진다. `colX`는 `columnsOf()`가 밀기까지 끝내 내보내고, `gapAt`·`bandAt`은
함수 안에 가둬 내보내지 않는다. 목적이던 두 자리가 이렇게 닫혔다.

`paint()` 안으로 노선도 스크롤을 들이면서 실행 순서가 한 번 바뀌었다(전에는 `syncPanel()`
뒤였다). 눈으로 확인할 수 없는 종류라 **스냅샷에 `scrollLeft`를 먼저 추가하고** 옮겼다.

### 검사를 부탁에서 장치로

`CLAUDE.md`에 "먼저 검사를 돌려라"라고 적어 둔 것은 부탁이다. 사람이 잊으면 그만이고,
Claude 세션이 아닌 경로 — GitHub 웹 편집, 다른 PC — 는 아예 안 걸린다. `.githooks/pre-commit`과
GitHub Actions로 같은 검사를 걸었다. 통과하면 훅은 아무 말도 하지 않는다 — 커밋마다 22줄이
쏟아지면 정작 실패했을 때 눈에 안 들어온다.

스냅샷은 **일부러 자동화하지 않았다.** 전후 비교 도구라 화면을 의도적으로 바꾸면 당연히
실패하고, 통과 조건으로 걸면 정상적인 개선마다 빨간불이 뜬다.

> **주석으로 지키던 규칙을 구조와 검사로 옮기는 것이 이번 작업의 전부다.** `render()`의
> 순서 의존성은 인자 관계가 되었고, "N군데를 함께 고쳐라"는 `check.mjs`가 되었고,
> "검사를 돌려라"는 훅이 되었다. 새 규칙을 쓸 때마다 한 단계 더 내려갈 수 있는지 보라.

### 남은 것

`HISTORY.md`를 이 항목 전까지 **빠뜨렸다.** 규칙·함정은 `CLAUDE.md`에 다 옮겼는데 기록만
빠졌다. `CLAUDE.md` 5절이 `HISTORY.md`의 존재만 적어 두고 "작업하면 여기 추가하라"를
말하지 않아서, 읽을 때만 여는 파일로 해석했다. 그 한 줄을 5절에 넣었다.

> 웹 세션에서는 `github.io` 접속과 태그 푸시(`refs/tags/*`)가 정책상 403으로 막힌다.
> **배포 결과 확인과 릴리스 태그는 로컬에서 해야 한다.** 스크린샷도 웹폰트가 뜨지 않은
> 상태로만 찍혔다 — 실제 학생 화면과 미세하게 다르다.

## 37차 — 36차 도구가 다른 PC에서 헛돌던 것

36차가 만든 도구를 **처음으로 다른 환경에서** 돌렸다. Windows에 clone하고
`node tools/check.mjs`를 치니 22개 중 3개가 어긋났다 — `render()`를 못 찾았고,
계산 네 국면이 없다고 했고, `colX`를 `columnsOf()` 밖에서 고친다고 했다.

셋 다 사실이 아니었다. `render()`는 1793행에, `graphOf`는 1407행에 멀쩡히 있었다.

원인은 한 줄이었다.

```js
const j = lines.findIndex((l, k) => k > i && l === "}");   // fnBody()
```

함수의 끝을 열 0의 `}`로 찾는데, Windows 체크아웃에서 그 줄은 실제로 `"}\r"`이다.
영영 안 맞으니 `fnBody()`가 본문을 통째로 못 꺼내고, 그 위에 선 검사 셋이 함께 무너진다.
`colX` 세 건은 파생이었다 — `columnsOf()`의 범위를 못 잡으니 **함수 안**의 줄을
밖으로 셌다.

`fnBody()`만 고칠 수도 있었지만, 줄 단위로 견주는 검사가 앞으로도 늘어날 자리다.
읽는 시점에 한 번 `\r\n`을 `\n`으로 맞췄다. 22개 모두 통과했다.

> **도구를 만든 환경에서만 검증하면, 다른 환경에서는 통째로 헛돌면서도 통과처럼
> 보인다.** 36차는 "일부러 망가뜨려 잡히는지"까지 확인했고 그건 옳았다. 다만 그
> 확인도 한 환경에서만 했다. 더 나쁜 건 이 실패가 **조용하지 않고 시끄러웠다**는
> 점이다 — 훅이 커밋을 막으니, 사정을 모르면 멀쩡한 코드를 검사에 맞춰 고치거나
> `--no-verify`를 습관으로 삼게 된다. 검사기가 틀렸을 때의 비용은 검사가 없을 때보다 크다.

같은 자리에서 하나 더 나왔다. `CLAUDE.md` 2절은 "`npm install` 한 번으로 훅이 걸린다
(`prepare`가 `core.hooksPath`를 잡는다)"라고 적어 두었는데, `package.json`에 그
`prepare`가 없었다. 문서를 믿고 설치만 한 사람은 훅 없이 커밋한다. 문서에 맞춰 넣고,
훅을 일부러 풀었다가 `npm install`을 다시 돌려 저절로 잡히는 것을 확인했다.

`name`도 함께 박았다. 없으면 npm이 **폴더 이름**을 lockfile에 적어, 저장소 이름과 다른
폴더에 clone한 사람마다 한 줄이 뒤집힌다(`GitMap` ↔ `GitBranchMap`).

## 38차 — 이력을 한 방향으로 펴고 번호를 다시 매긴다

번호가 `1…8 → 36 → 35 → … → 9`로 뒤집혀 있었다. 파일을 열면 1차부터 8차를 지나 갑자기
36차를 만나고 거기서부터 거꾸로 내려간다.

만들어진 경위를 찾아봤다. 이 파일은 `CLAUDE.md`에서 떨어져 나올 때 **1차부터 9차까지 전부
오름차순**이었다. 지금 8차와 최신 사이에 낀 그 9차가 당시에는 맨 아래 마지막 항목이었다.
10차를 쓰는 시점에 "9차 위에 넣는" 방침으로 바뀌었고, 이미 쓰인 1~8차는 뒤집지 않아
그 부분만 옛 순서로 굳었다.

파일 전체를 오름차순으로 펴고 번호를 다시 부여했다. 앞선 이력 하나가 빠져 있던 것도
이때 채웠다 — 태그 `v1.0`·`v1.0.1`로 나간 디자인 리뷰 수정이 기록되지 않은 채
그 위로 두 판이 더 쌓여 있었다. 조상 관계로 자리를 확정해(`v1.0 → v1.0.1 → v1.1`)
34차로 끼워 넣었고, 뒤의 셋이 35·36·37차로 밀렸다.

**번호를 건드릴 수 있었던 이유는 아무도 번호로 부르지 않고 있었기 때문이다.** `CLAUDE.md`와
`README.md`에서 `N차` 참조가 0곳이었고(예전에는 "22차에 수정"처럼 불렀지만 그 사이 개편에서
사라졌다), 이 파일 안에서는 32차를 두 번 부르는 것뿐이었다 — 32차는 번호가 안 바뀌었다.
37차 제목과 본문이 "35차 도구"라고 부르던 세 곳만 36차로 고쳤다.

> **번호를 식별자로 쓰기 시작하면 다시는 못 바꾼다.** 이번에 정리가 가능했던 것은
> 운이었다. 앞으로 이력을 부를 일이 있으면 번호보다 제목이나 커밋 해시로 부르는 편이
> 안전하다.

정렬은 손으로 하지 않고 스크립트로 했다. 37개 항목을 잘라 번호로 정렬하고 다시 붙이는
동안 **항목 수와 번호 연속성을 검증**하게 해 두었다 — 900줄을 손으로 옮기면 한 항목이
조용히 사라져도 모른다.
