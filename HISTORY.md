# 작업 이력

`CLAUDE.md`에서 분리한 변경 내역. 지침이 아니라 기록이다.
현재 유효한 규칙·함정은 `CLAUDE.md` 쪽에 있다.

원본은 HEAD 표시·이름표 겹침·모바일 미검증 3개 버그를 가진 상태였다. 아래는 그 이후 전체 변경 내역.

## 1차 — 최초 3개 버그

**A. HEAD가 어느 브랜치를 가리키는지 안 보임 (최우선)**
`git switch -c` 직후 이름표만 두 개 붙고 HEAD는 커밋 아래 그대로 있어, 안내 문구("HEAD가 새 노선으로 옮겨갔다")와 그림이 어긋났다.

- 커밋 아래 HEAD 뱃지를 없애고 **HEAD → 브랜치 이름표 → 커밋** 3단 구조로 변경
- 이름표를 커밋별로 묶어(`labels`) 오른쪽에 세로로 쌓고 연결선으로 커밋과 연결
- HEAD 뱃지는 현재 브랜치 이름표 오른쪽에 붙고 화살표가 그 이름표를 가리킨다
- 현재 브랜치 = 진한 테두리 + `font-weight:800` + 불투명 / 나머지 = `opacity:.5`

**B. 브랜치 이름표 겹침**

- `ROW_H`를 상수에서 계산값으로 (쌓인 개수에 따라 확대)
- 열 간격 동적 확대 (`gapAt`)
- 이름표 뒤 흰 바탕 rect — 선로 위에 얹혀도 읽힘
- `maxX`/`maxY`가 이름표+HEAD 폭까지 포함하도록 수정
- 커밋 없는 lane의 안내선은 그리지 않음

**C. 모바일 미검증**

- `tourPlace()`: 대상이 화면 밖이면 `scrollIntoView`, 말풍선은 여백 넓은 쪽에, 부족하면 `scrollBy`로 자리 확보 (기존엔 스크롤을 안 해서 390px에서 조작 패널 단계 스포트라이트가 화면 밖이었다)
- 노선도 가로 자동 스크롤: 기본은 최신 역, HEAD가 왼쪽으로 잘리면 HEAD 쪽으로

## 2차 — rebase 유령 커밋 + 리뷰 발견분

- **rebase 후 버려진 커밋이 새 커밋에 가려지던 문제** → `rowKey`로 살아있는 줄/버려진 줄을 분리. 부수 효과로 빈 lane 압축도 됨
- 자동 안내 2단계 문구 오류 정정 (해시/메시지 위아래 순서가 실제와 반대였다)
- **안내를 닫아도 예약된 단계 동작이 실행되던 버그** — `clearTimeout(tTimer)`가 죽은 코드였다. `tLater`/`tClear`로 예약 타이머를 모아 관리
- **XSS** — 커밋 메시지·브랜치 이름이 `innerHTML`로 들어갔다. `esc()` 추가
- 빈 커밋 메시지로 커밋하면 로그에 `git commit -m ""`이 남던 문제
- 브랜치 이름 검증 추가 (git `check-ref-format` 핵심 규칙, 한글은 실제 git처럼 허용)
- 현재 브랜치로 `이동` 시 무반응 → 안내 문구
- `▶ 자동 안내` 버튼에 `aria-pressed`가 붙던 문제
- 범례에 HEAD 항목 추가

## 3차 — rebase 정확성 + 안내 재실행

- **rebase가 병합 커밋을 복제하던 문제** → 실제 git처럼 병합 커밋은 replay에서 제외 (`--rebase-merges` 없을 때의 동작). 버려진 병합 커밋은 자동으로 점선 줄로 내려간다
- 안내에서 뒤로 갔다 앞으로 오면 단계 동작이 중복 실행되던 문제
- 버려진 병합 커밋의 가운데 점만 원색으로 남던 시각 결함

## 4차 — 안내 재생 제어

- **이전 버튼을 눌러도 노선도가 되감기지 않던 문제** → 단계 진입 시점의 상태를 `tSnap[]`에 스냅샷하고 복원. 동작이 있는 단계는 재실행되므로 이후 스냅샷을 무효화. 이 구조 덕분에 3차의 중복 실행 방지 플래그(`tRan`)는 제거됨
- **일시정지해도 단계 시계가 계속 흘러, 이어보기를 누르면 바로 다음 단계로 넘어가던 문제** → `tT0`/`tPauseAt`로 멈춘 시간만큼 시작 시각을 미룸

## 5차 — 안내 말풍선 위치

- 여백 넓은 쪽으로 위아래를 오가던 말풍선을 **노선도 패널 바로 아래에 고정 여백 20px**로 고정 (넓은 화면)
- 가로 중앙도 화면이 아니라 **노선도 폭 기준** — 화면 중앙에 두면 오른쪽 조작판을 30px 물었다
- 좁은 화면(<900px)은 붙일 빈 자리가 없어 화면 아래 22px 고정 + 페이지 여백 확보 방식 유지
- 뷰포트가 낮으면 여백이 줄어들 뿐 말풍선이 화면 밖으로 나가지 않도록 클램프

## 6차 — 10단계 서사 불일치

안내 10단계가 "팀원이 main에 커밋했다"고 설명하면서 실제로는 내가 main에서 커밋하고 있었다. 로그에는 `git switch main` + `git commit`이 남는데 19단계에서 "이 로그를 그대로 터미널에 치면 같은 결과"라고 말하는 자료라 그냥 둘 수 없었다.

1차로 문구를 "`git pull`로 받아오지만 여기서는 흉내 낸다"로 고치고 관찰 단계를 추가했으나, 사용자 지적으로 **실제 원격 저장소를 구현**하는 방향으로 확대.

## 7차 — 원격 저장소 구현

- `origin/main`을 `remote:true` 브랜치 항목으로 추가. 추적 대상은 main 하나 (실제 git도 새 로컬 브랜치는 push 전까지 원격에 없다)
- 선로 색은 main과 동일 ("노선 = 색" 규칙 유지), **속이 빈 이름표**로만 원격을 구분
- 자유 모드 버튼 3개: `팀원이 커밋` / `받아오기 · pull` / `올리기 · push`
  - pull: 뒤처졌으면 fast-forward, 갈라졌으면 병합 커밋 (`git pull` = fetch + merge)
  - push: 원격이 조상이면 전진, 아니면 **거절**(non-fast-forward, 로그에 빨강)
  - 가드: main이 아닌 브랜치, 이미 최신, 올릴 것 없음, 내 쪽이 앞섬
- 시나리오 모드(Merge/Rebase)는 원격을 만들지 않는다 (`fresh(withRemote)`)
- **렌더러 버그 2건 수정** (이 작업에서 드러남)
  - 원격이 앞선 상태에서 로컬 커밋 시 두 커밋이 같은 칸에 겹치던 문제 → `doCommit`에 갈라짐 규칙 추가 (같은 줄에 이미 선로가 뻗어 있으면 새 줄로, 색은 유지)
  - 열 간격 계산이 "부모-자식인 다음 역"만 봐서, rebase 후 `origin/main`이 무관한 다음 역과 겹치던 문제 → **같은 줄 다음 칸이 차 있는지**로 판정하도록 교체
- 안내 21단계로 재구성. **초반 push 단계가 없으면 origin이 뒤처져 12단계 pull이 fast-forward가 아니라 병합이 되어 버린다**는 문제를 발견해 6단계에 push를 넣어 해결

```
4 커밋 → 5 관찰 → 6 원격에 올려두기(push) → 7 브랜치 → 8 관찰 → 9 기능 커밋 2회
→ 10 main으로 이동 → 11 팀원이 원격에 커밋 → 12 원격만 앞서 나감(관찰)
→ 13 받아오기(pull, fast-forward) → 14 두 노선 갈라짐(관찰) → 15 병합 → 16 환승역(관찰)
→ 17 올려서 공유(push) → 18 기호 → 19 로그 → 20 시나리오 → 21 마무리
```

결과적으로 로그가 그대로 재현 가능한 협업 세션이 되었고, "흉내 냅니다" 류의 면피 문구가 사라졌다.

## 8차 — 저장소 구성

- `git init` → GitHub `abback-go/GitMap`(PUBLIC, 비어 있던 상태)에 푸시
- README 작성 → 이후 **학생용으로 재작성** (맨 위 바로 열기 링크, 자동 안내부터 시작하는 순서, 화면·기호 읽는 법. 개발 내용은 `<details>` 접이식)
- GitHub Pages 활성화 (`main` 브랜치 root)
- `git-branch-map.html` → **`index.html`로 이름 변경** — Pages 루트에서 바로 열리도록. 학생 링크가 짧아졌다
- `develop` 브랜치 생성

## 18차 — 시나리오 · Clone (다른 PC에서 이어받기)

지금까지는 **빈 저장소에서 시작해 만들어 올리는** 방향만 다뤘다. 정작 학생이 처음
겪는 건 반대쪽 — **이미 있는 저장소를 clone해서 참여하는** 상황이다. 그 조각을 채웠다.

- 시나리오 5단계: `git clone` → `git branch -a` → `git switch develop` →
  `git commit` → `git push`
- **3단계가 핵심.** clone하면 로컬 브랜치는 기본 브랜치 하나뿐이라
  `origin/develop`은 보이는데 `develop`이 없다. `cmdSwitch`에 **로컬에 없고 원격에만
  있는 이름** 경로를 추가해, git이 추적 브랜치를 자동으로 만들어 주는 걸 보여준다.
  기존 `cmdSwitch`는 `if(!S.branches[name]) return;`으로 **조용히 막고 있었다**
- `freshClone()` — 서버에만 저장소가 있고 로컬은 빈 상태. `S.head = ""`가
  "아직 내 저장소가 없다"는 뜻이다. 서버는 `origin/main` 2커밋 + 거기서 갈라진
  `origin/develop` 1커밋
- 새 명령 `cmdClone()`(원격 전체 복사 + 기본 브랜치 생성), `cmdBranchList()`
  (상태를 안 바꾸는 조회. 서버에만 있는 노선을 눈으로 확인시킨다)
- **빈 로컬에서 `render()`가 터지던 곳 2군데.** `cur()`가 `undefined`가 되어
  `cur().head`에서 죽었다 → `c0 && c0.head`로 방어. `nowBranch`는 "아직 없음" 표시
- **`git init` 머리말이 clone 시나리오에서는 틀린 말이다.** 이미 있는 저장소를
  받아오는 이야기라 1단계 `git clone` 자체가 "처음 한 번만" 하는 일이다.
  `mode === "clone"`이면 머리말을 숨긴다
- `scenario.intro` — 시작 화면이 여느 시나리오와 다를 때만 따로 설명
- `.modes`에 `flex-wrap:wrap` — 버튼이 5개가 되어 390px에서 넘쳤다. 두 줄로 감싼다

**clone 전인데 로컬에 선로가 그려져 있던 문제.** 처음엔 문구로 때웠는데
("위쪽에 이름표가 하나도 없는 게 증거입니다") 지적을 받았다. 맞는 지적이다 —
교육 자료에서 그림이 사실과 다르면 안 된다.

원인은 `gotten` 계산이었다. "내가 받은 커밋"을 `원격 known ∪ 로컬 head`의 조상으로
구하는데, clone 전에도 원격 `known`이 채워져 있어 전부 "받은 것"으로 판정됐다.
**`S.head`가 비어 있으면(= 내 저장소가 없으면) `gotten`을 통째로 비우도록** 고쳤다.
한 줄이다.

```js
if(S.head) Object.values(S.branches).forEach(...)
```

그러면 서버 커밋 전부가 기존 문법 그대로 **"아직 받지 않은 역"**(`REMOTE_COLOR` +
파선 `7 5`)으로 그려진다. `known`을 비우는 방법은 쓰지 않았다 — `rlabels`가 `known`에
붙으므로 서버 띠 이름표가 통째로 사라지고(`if(!at) return`), 2부의 `fetch`
개념(`known ≠ head`)도 깨진다.

덤으로 **clone의 효과가 눈에 보이게 됐다.** 파선이던 역 3개가 한 번에 진해진다.
`intro`와 `cmdClone`의 문구도 "파선 → 진해짐"을 가리키도록 고쳤다.

**"clone 한 번에 develop 역까지 진해지는 게 맞나"** — 맞다. 실제 git으로 확인했다.
임시 서버(`main` 2커밋 + `develop` 1커밋)를 만들어 clone한 뒤 **서버를 지우고**
조회했더니 `git log origin/develop`도, `git show origin/develop:c.txt`도 그대로
나왔다(`git fetch`만 실패). clone은 **모든 브랜치의 커밋과 파일을 전부** 받아온다.
안 따라오는 건 **로컬 브랜치(이름표)** 뿐이다.

다만 그 위화감이 이 시나리오의 교육 포인트라서, 1단계 문구가 짚고 넘어가도록 고쳤다 —
"커밋은 다 왔는데 브랜치는 안 따라왔습니다". 2단계 `git branch -a`로 이어진다.

**`git branch -a` 단계에서 그림이 안 바뀌어 고장난 것처럼 보였다.** 조회 명령이라
저장소를 안 바꾸는 게 맞지만, "다음 단계"를 눌렀는데 화면이 그대로면 학생은 멈춘
줄 안다. 실제 터미널이 그렇듯 **로그에 목록을 뿌리도록** 했다. 로그 종류에 `"o"`
(명령이 뿌린 출력)를 추가 — `$`도 `#`도 없는 흐린 줄이라 옮겨 칠 대상이 아님이
형태로 구분된다.

```
$ git branch -a
* main
  remotes/origin/main
  remotes/origin/develop
```

문구도 "**그림은 그대로입니다** — 조회만 하는 명령이라 저장소를 바꾸지 않습니다"로
시작하게 고쳤다. 안 바뀌는 게 정상임을 먼저 말해 준다.

**"`git switch`는 서버에 없어도 브랜치를 만드는 명령 아닌가"** — 아니다. git 2.52로
세 경우를 확인했다.

| 상황 | `git switch develop` |
|---|---|
| 로컬·원격 둘 다 없음 | `fatal: invalid reference: develop` — 거절 |
| 원격에만 있음 | `set up to track 'origin/develop'` + `Switched to a new branch` |
| 새로 만들기 | `-c`가 필요. 없으면 똑같이 거절 |

**진행 순서에 `git add .`이 빠져 있었다.** 커밋 단계가 전부 `cmdCommit(msg, true)`
(담기까지 한 번에)라 로그에는 `git add .`이 찍히는데 진행 순서에는 없어서, 두 칸이
어긋나 보였다. **clone만이 아니라 merge·rebase도 같은 불일치**를 갖고 있었다.

`steps[].cmd`가 **배열도 받도록** 했다. 배열이면 여러 줄로 그려지고 한 번의
"다음 단계"에 **함께 체크**된다. 실행은 그대로 한 번이다.

```js
{ cmd:['git add .','git commit -m "보스 패턴 추가"'], run:()=>cmdCommit("보스 패턴 추가", true) }
```

세 시나리오 모두 진행 순서와 로그가 1:1로 맞는 걸 확인했다(clone 6줄 / merge 11줄 /
rebase 14줄). rebase 마지막 줄만 로그에 `   # fast-forward`가 더 붙는데, 이건 명령이
아니라 결과 주석이고 bash에서도 주석이라 그대로 옮겨 쳐도 동작한다.

앱은 이미 두 명령을 구분하고 있었다 — **브랜치 + 이동** 버튼이 `git switch -c`,
**이동** 버튼이 `git switch`. 다만 3단계 문구가 "서버에도 없으면 어떻게 되는지"를
빼먹어 오해를 샀다. "서버에 있을 **때만**", "`git switch`는 브랜치를 **만드는** 명령이
아닙니다"를 넣었다. `cmdSwitch`의 **조용한 거절**(`if(!r) return;`)에도 실제 git 메시지를
띄우도록 했다 — UI로는 닿기 어려운 경로지만 조용히 아무 일도 안 일어나는 건 이 앱이
반복해서 겪은 함정이다.

검증: 1440px·390px 양쪽, 5단계 전 구간 거절 로그 0건, 기존 3개 모드 회귀 없음
(clone → 자유 모드 복귀 시 머리말·원격 정상 복구).

## 17차 — Codex 2차 검수 반영

Critical 없음. Major 2 + Minor 3, 전부 확인 후 수정.

- **로그 재현 약속이 여전히 안 지켜졌다.** 머리말에 `git init` / `git remote add`만
  넣었는데, ① 최초 커밋 기록이 없고 ② upstream 설정이 없어 첫 `git push`가
  `no upstream branch`로 실패한다. 머리말을 다섯 줄로 늘려
  (`init` → `add` → `commit "최초 커밋"` → `remote add` → `push -u origin main`)
  해결. 팀원 커밋·PR은 `#` 주석이라 애초에 명령이 아니므로, 문구를
  "`$`로 시작하는 줄을 옮겨 치면 된다 / `#`은 팀원이나 GitHub이 한 일"로 갈랐다
- **원격 기능 브랜치가 둘 이상이면 PR이 삽입 순서상 첫 번째를 골랐다.** 사용자가
  고를 방법이 없었다. `selPR` 드롭다운 추가(아직 안 합쳐진 원격 브랜치만 채운다),
  `cmdPR(name)`으로 명시 전달. 이미 합쳐진 이름을 지정하면 거절
- 안내 되감기가 `S`만 복원하고 입력칸은 그대로였다 → `tourCapture`/`tourRestore`에
  `#msg`, `#bname` 포함
- 담긴 변경 칩에 `role="status"` `aria-live="polite"`
- 8절 기호 규칙 수치가 코드와 달랐다(선로 `0.1 7`, opacity가 역/선로/범례 제각각).
  표를 역·선로로 나눠 실제 값으로 고치고 범례 opacity를 `.85` → `.8`로 통일

## 16차 — 안내 PR 단계 + 범례 아이콘 구분

- 안내 24 → 25단계. PR과 로컬 merge는 같은 브랜치에 둘 다 못 쓰므로(먼저 합친
  쪽이 이긴다) 안내 본선은 PR, 로컬 merge는 시나리오 모드가 전담하도록 나눴다
- **범례에서 `버려진 커밋`과 `아직 받지 않은 역`이 둘 다 점선 원**이라 색으로만
  구분되던 문제(7절 미결). 선 종류를 갈랐다 — 버려진 커밋은 **점**
  (`0.1 5` + round cap), 아직 안 받은 역은 **파선**(`7 5`). 지도와 범례를 함께
  바꿨다. 한쪽만 바꾸면 어긋난다
- `CLAUDE.md`에 8절 기호 규칙 표 추가, 낡은 단계 번호(21 → 22) 정정

## 15차 — PR 병합

7차에서 "PR은 git이 아니라 GitHub 기능"이라며 제외했는데, 서버 띠와
기능 브랜치 push가 생기면서 판단을 뒤집었다. PR은 **다른 어떤 기능도
못 가르치는 것**을 가르친다 — 병합이 내 컴퓨터가 아니라 서버에서
일어나고, 나는 그 결과를 받아와야 한다는 것. 그전까지 서버 띠에서
자발적으로 일어나는 일은 하나도 없었다.

- `cmdPR()` — 서버에서 `origin/<기능>`을 `origin/main`에 병합해 환승역을
  만든다. 로컬 브랜치도 HEAD도 안 움직이고, `known`도 그대로라 새 역이
  "아직 받지 않은 역"으로 흐리게 뜬다. 이어서 fetch/pull이 필요해진다
- 로그는 `# GitHub에서 Pull Request를 병합했습니다` 설명 줄. git 명령이
  아니므로 "복붙하면 재현된다" 약속이 안 깨진다
- 가드: 원격 없음 / 서버에 기능 브랜치 없음 / 이미 합쳐짐
- 리뷰·코멘트·승인 UI는 안 만든다. GitHub 화면이지 저장소 상태가 아니다

**안내 24 → 25단계.** PR과 로컬 merge는 같은 브랜치에 둘 다 못 쓴다(먼저 합친
쪽이 이기고 나머지는 "이미 합쳐졌다"로 거절된다). 그래서 안내 본선은 팀 협업
흐름인 PR로 가고, 로컬 `git merge`는 시나리오 모드 두 개가 전담하도록 나눴다.

```
17 두 노선 갈라짐(관찰) → 18 PR로 합치기 → 19 합쳐진 곳은 서버입니다(관찰)
→ 20 합친 결과 받아오기(pull) → 21 환승역이 생겼습니다(관찰)
```

빠진 것: 기존 18 `병합하기`(로컬 merge), 기존 20 `올려서 공유`(push — PR 뒤에는
서버가 이미 앞서 있어 올릴 게 없다). 17단계와 24단계 문구에 "합치는 길은 둘"과
"시나리오에서 로컬 방식을 본다"를 넣어 연결했다.

작업 중 발견해 함께 고친 것 2건:

- `.staged{display:flex}`가 `[hidden]` 기본값을 덮어써 **담긴 변경 칩이
  항상 보였다.** `.staged[hidden]{display:none}` 추가
- 아직 받지 않은 병합 커밋의 **가운데 점만 원색**으로 남았다(3차에서
  버려진 커밋에 대해 고쳤던 것과 같은 결함). 바깥 원과 같은 상태로 그린다

## 14차 — 스테이징(git add) 최소 모델

로그에 `git switch -c dev` → `git commit`이 연달아 남는데, 실제 git은
`git add` 없이는 커밋되지 않는다. 22단계의 "이 목록을 그대로 터미널에
치면 같은 결과"라는 약속이 깨져 있었다.

`git add .` 한 줄을 자동으로 끼워 넣는 대신, 사용자가 실제로 담는
동작을 하게 했다. 그래야 로그가 "내가 누른 것"의 기록으로 남는다.

- `S.staged` 추가. 입력칸 내용 = 내가 한 작업, `담기 · add`로 담는다
- 담지 않고 커밋하면 **거절**하고 로그에 빨강으로 남긴다
  (`git commit -m "..."   # nothing added`). 초보자가 가장 많이 겪는
  실패를 그대로 겪게 하는 게 이 모델의 목적이다
- 커밋하면 담긴 것이 비워지고 역이 생긴다
- 시나리오·자동 안내는 `cmdCommit(msg, true)`로 담기까지 자동. 24단계
  서사와 진행 순서 목록은 그대로 두되 **로그에는 `git add .`가 남는다**
- 스테이징은 조작 패널 안에서만 표현한다. 노선도에는 넣지 않았다 —
  역이 아닌 것을 지하철 은유에 넣으면 그림이 무너진다

이어서 `git init` 이야기가 나왔는데, 넣는다면 `git remote add origin`도 같이
넣어야 앞뒤가 맞는다(원격 설정 없이는 push 줄이 여전히 거짓말이다). 빈 저장소
모드를 만드는 대신 **로그 맨 위 고정 머리말**로 처리했다.

- `# 처음 한 번만 — 저장소를 만들 때` 아래 `git init` / `git remote add origin`
  두 줄을 흐리게 고정 표시하고 점선으로 아래 기록과 구분
- 전사가 완전해지고, "이건 매번 하는 게 아니다"를 형태로 가르친다
- 앱 상태·노선도·안내 구조는 안 건드렸다. init은 평생 한 번이라
  버튼과 빈 화면을 만들 값어치가 없다고 판단

## 13차 — Codex 최종 검수 반영

리뷰에서 Major 3건 + Minor 1건. 전부 브라우저에서 재현 확인 후 수정.

- **진행 막대로 앞 단계를 누르면 커밋이 중복 생성**됐다. `tourJump`가 현재 상태 위에 이전 단계 동작을 덧실행했기 때문. 재현: 4단계 실행 → 5단계 → 막대에서 4단계 클릭 → `적 AI 추가`가 두 개. `tBase`(안내 시작 스냅샷)를 저장하고 건너뛸 때 먼저 복원하도록 수정
- **스크롤하면 스포트라이트가 안 따라왔다.** 파고들어 보니 원인은 `resize`만 듣던 게 아니라 **DOCTYPE 누락으로 quirks mode**였던 것 — `scrollingElement`가 `BODY`가 되어 window `scroll` 이벤트가 아예 안 잡혔다. `<!DOCTYPE html>` 추가 + `scroll` 리스너 연결(`tourSync`는 스크롤을 다시 건드리지 않고 자리만 맞춘다). 표준 모드 전환 후 1440px·390px 레이아웃 회귀 없음 확인
- **한글 브랜치 이름이 알약을 넘쳤다.** 폭을 `글자수 × 7.6`으로 어림잡았는데 Pretendard가 고정폭이 아니다. `기능개발브랜치입니다`가 실제 99.4px인데 알약은 96px. canvas `measureText`로 실측하도록 교체, 웹폰트 로드 후 재측정
- README에서 서버 띠 설명이 반대로 읽히던 것 수정 (띠 이름표는 서버의 현재 위치가 아니라 내가 마지막 fetch로 확인한 위치)

## 12차 — 미결 정리

- **rebase를 두 번 하면 유령 커밋이 겹치던 문제 재현·수정.** 검증해 보니 실제로 두 역이 같은 좌표(290,296)에 놓여 하나가 가려졌다. `rowKey`가 lane으로만 묶는데 두 세대의 유령이 같은 lane을 쓰기 때문. rebase 때 버려지는 커밋을 새 lane(`ghostLane`)으로 옮겨 세대별로 줄을 나눴다. 수정 후 3줄 → 4줄, 중복 좌표 0
- **파비콘 추가.** 인라인 SVG data URI라 단일 HTML 제약을 유지한다. 갈라지는 두 노선과 환승역 모양
- CLAUDE.md 3·4·7절을 현재 코드에 맞게 갱신, README를 24단계·서버 띠·fetch 기준으로 수정

## 11차 — 안내 3장 구성

2타임 연속 강의용. 안내를 쪼개지 않고 장 표시만 붙였다.

- `ch` 필드는 장 시작 단계에만. 나머지는 `TOUR_CH`가 이어받으므로 단계를 넣고 빼도 경계가 안 어긋난다
- 진행 막대를 단계당 한 칸으로 쪼개고 장 경계에 틈. 칸을 누르면 `tourJump`
- `tourJump`는 지나친 단계의 동작을 재생하고 스냅샷도 채워 되감기를 유지한다
- **`tLater` 예약이 건너뛰기에서 지워지던 버그** — 9단계의 두 번째 커밋("이단 점프")이 통째로 빠졌다. `tJumping` 플래그로 즉시 실행
- `#step=N` 해시로 재개. `localStorage` 금지라 주소에 남긴다
- 쉬는 시간 경계는 11 → 12단계

## 10차 — 기능 브랜치도 서버에

- 서버 띠에 `origin/main` 하나만 보여서 "브랜치는 내 컴퓨터에만 있는 것"이라는 오해를 남겼다
- `RPFX` 접두사 기반으로 원격을 일반화. `cmdPush`가 원격 없는 브랜치를 만나면 `git push -u origin <이름>`으로 새로 만든다
- 선행 조건이던 레이아웃 버그 2건 수정 — 같은 역의 이름표 묶음 가운데 정렬, 좌우 끝을 `maxX`·`gapAt`에 반영, 왼쪽 넘침은 `colX` 전체 이동으로 보정
- `remoteGuard`의 main 전용 제약 제거

## 9차 — 문서 정리

- `CLAUDE.md`에 표준 헤더 추가, 작업 이력을 이 파일로 분리
- 검증 절차를 Playwright 전용에서 브라우저 MCP 일반(Playwright / claude-in-chrome)으로 고쳐 씀
- 전역 조작 함수 표를 `CLAUDE.md` 3절에 추가
- 루트에 남아 있던 `before-desktop.png`를 `shots/`로 이동
