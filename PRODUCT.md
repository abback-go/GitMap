# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 1차: 명지전문대 AI게임학과 수강생 — git을 처음 배우는 입문자. 강의 중 교수가 프로젝터/큰 화면에 띄워 시연하는 것을 보고, 이후 실습실 공용 PC나 집에서 직접 눌러 본다.
- 2차: 링크로 접한 일반 git 입문자 — 공개 교육 자료로 운영한다(사용자 확인). 설치·가입 없이 링크 하나로 열리는 것이 전제.

## Product Purpose

Git 브랜치 개념(커밋·브랜치·병합·rebase·원격 동기화)을 지하철 노선도 은유로 시각화하는 교육용 웹앱. 커밋은 역, 브랜치는 노선, 병합 커밋은 환승역.

성공 기준: **터미널 실전 전이** — 학생이 화면의 로그에 쌓인 명령을 실제 Git Bash에 그대로 옮겨 쳐서 같은 결과를 내는 것. 버튼 라벨과 실제 git 명령이 1:1로 맞는 것이 이 기준을 떠받친다. (사용 장면·성공 기준은 인터뷰로 확인, 성공 기준은 "추정" 선택지로 확인됨)

## Positioning

- 일반 git 시각화 도구와 달리 "무엇이 어디에 있는 커밋인가"를 화면 구역(내 컴퓨터 위 / 팀 공용 서버 아래 회색 띠)으로 공간화한다. push·fetch·pull이 경계를 넘는 동작으로 보인다.
- `origin/main` 이름표가 서버의 현재 위치가 아니라 "내가 마지막 fetch로 확인한 위치"라는 것까지 구분해 그린다 (원격은 head/known 이중 상태).
- **거절도 커리큘럼이다** — `nothing added to commit`, non-fast-forward push 거절을 일부러 겪게 하는 시나리오 설계.

## Operating Context

- 강의 중 교수 시연이 주된 장면(사용자 확인) — 뒷자리에서도 읽혀야 한다. 거절 로그의 배경색 등은 이 요구에서 나왔다.
- 실습실 공용 PC: 앞 사람 상태가 남으면 안 됨 → `localStorage`/`sessionStorage` 금지, 매 세션 깨끗하게 시작. 이어보기는 URL 해시(`#step=13`).
- 오프라인 수업 대비: `index.html` 하나만 내려받아 열어도 동작 (외부 자원은 Pretendard CDN뿐, 없으면 시스템 글꼴).
- 데스크톱 1440px과 모바일 390px 양쪽 지원.

## Capabilities and Constraints

- 의존성 없는 단일 HTML 파일. 외부 JS/CSS 분리 금지 (Claude 아티팩트 이식 염두).
- 자동 안내 30단계 3부 구성 + 학습 시나리오 7종(번호 = 학습 순서) + 자유 모드.
- UI는 한국어, 단 git 명령에 대응하는 버튼 라벨만 명령어 원문 (`add`, `commit`, `push` 등). git 명령이 아닌 버튼은 한글 — 한글 자체가 "git 명령이 아니다" 신호.
- 다루지 않는 범위(의도적): 팀원의 기능 브랜치 커밋, 원격 브랜치 삭제/`--prune`, PR 리뷰·코멘트 UI.
- 상세 규칙·좌표 체계·알려진 함정은 CLAUDE.md가 관리한다.

## Brand Commitments

- 지하철 노선도 은유 — 디자인의 핵심, 변경 금지.
- 기존 색 팔레트와 Pretendard 폰트 유지.
- 이름표 알약 글씨색은 `pillText()` 대비 계산, 노선 색 자체는 은유의 핵심이라 불변.

## Evidence on Hand

- 배포: https://abback-go.github.io/GitMap/ (GitHub Pages, `main` 브랜치).
- 저장소: https://github.com/abback-go/GitMap
- 본체: `index.html` 단일 파일. 사용 안내·기호 설명은 `README.md`, 변경 이력은 `HISTORY.md`.
- 학생 사용 데이터·후기 없음 — 성과 주장을 지어내지 말 것.

## Product Principles

1. **버튼 = 명령.** 화면에서 누르는 것과 터미널에 칠 것이 1:1. 라벨·로그·안내 문구가 이 대응을 깨면 안 된다.
2. **위치가 의미다.** 커밋이 어느 구역(내 컴퓨터/서버)에 어떤 선(실선/파선/점선)으로 그려지는가가 곧 개념 설명이다.
3. **거절은 고장이 아니라 교재다.** 실패 경로를 숨기지 않고 일부러 겪게 한다.
4. **아무것도 안 변하는 단계도 가르친다.** `git branch -a`, `remote add`, PR 열기처럼 저장소가 안 바뀌는 명령의 "안 바뀜" 자체가 교육 포인트.
5. **뒷자리 가독성.** 시연이 주 장면이므로 원거리에서 상태 변화가 읽혀야 한다.
